interface:
  display_name: "Orochi Release and Deploy"
  short_description: "Release and hard-restart both mesh nodes"
  default_prompt: "Use $orochi-release-deploy to verify, deploy, hard-restart, accept, document, and publish Orochi in order."
