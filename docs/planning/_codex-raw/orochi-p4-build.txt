You are a distributed-systems + protocol architect with 100% design freedom. Design — from scratch,
in Zig — the PROTOCOL LAYER for "Orochi", a clean-slate Zig successor to the ophion IRC daemon:
(A) the client-facing IRC line protocol, and (B) the SERVER-TO-SERVER "step up" replacing legacy
TS6 with the native LADON + VEIL cryptographic mesh. Everything Zig-native; NO C interop. Read the
shared brief first: /home/kain/orochi/docs/BRIEF.md

MANDATE: invent. The LADON/VEIL specs are the conceptual FOUNDATION — read them faithfully, then
innovate beyond them where a from-scratch design allows. ophion/libop are reference only.

Study (cite file:line / spec section):
- Client protocol: /home/kain/ophion/ircd/msgbuf.c, msgbuf_parse.c, parse.c, send.c, numeric.c,
  include/msgbuf.h, modules/m_cap.c
- LADON spec: /home/kain/ophion/docs/protocols/ladon/ (read EVERY file)
- VEIL spec: /home/kain/ophion/docs/protocols/veil-security.md
- LADON media modules: /home/kain/ophion/docs/reference/modules/m_ladon_*.md (voice/video transport)
- libop math substrate (reference for the Zig redesign): /home/kain/libop/include/op_crdt.h,
  op_vclock.h, op_hlc.h, op_merkle.h, op_bloom.h, op_graph.h

Produce a thorough design doc "04 — Protocol: Client Line + LADON/VEIL Mesh":

PART A — Client line protocol in Zig:
1. Line + IRCv3 tag parsing (SIMD-accelerated, zero-copy); numerics; cap-gated tag emission.
2. Send-path batching and backpressure.

PART B — LADON + VEIL mesh (the heavy, inventive part):
3. Faithfully summarize what the LADON and VEIL specs ACTUALLY say (cite sections; do not invent
   in this step), then clearly mark where you propose to go BEYOND them.
4. Node identity, trust, admission control, capability tokens (Ed25519).
5. VEIL secure channel end-to-end (handshake, per-frame crypto, ratchet, replay) at the protocol
   level (crypto primitives are designed elsewhere; here define the protocol/state machine).
6. Transport framing (msgpack or a new compact format? propose) and how it rides the event loop.
7. Membership: SWIM-style gossip — failure detection, suspicion, join/leave, large-mesh scaling.
8. STATE MODEL as CRDTs: choose a CRDT type per IRC entity (nicks, channels, memberships, modes,
   bans, topics, accounts) and JUSTIFY each. Solve the hard cases: mode conflict resolution,
   nick uniqueness (collision = RENAME loser to its UID, never kill), channel TS/ownership, ban
   merges, account authority. Consider delta-state CRDTs to bound bandwidth.
9. Anti-entropy: Merkle delta sync + vector/hybrid-logical clocks; gossip vs pull; convergence
   bounds.
10. Partition / split-brain / heal semantics of a MESH vs a spanning tree — operator + user impact.
11. Voice/video data transport (the LADON media modules) — architecture at protocol level.
12. Migration: clean break vs transitional bridge — recommend.
13. NOVEL TECHNOLOGIES: 3-6 protocol inventions (e.g. a new compact self-describing wire format,
    causal-broadcast optimizations, verifiable gossip, partition-aware UX). Name + spec.
14. Threat model & risks: Byzantine nodes, replay, CRDT metadata growth, convergence latency,
    user-visible ordering anomalies.

Be rigorous and bold; cite real spec sections + libop symbols. Include concrete data-structure
sketches. PLANNING ONLY — do not modify any files.
