interface:
  display_name: "Onyx Server Release and Deploy"
  short_description: "Release and hard-restart both mesh nodes"
  default_prompt: "Use $onyx-server-release-deploy to verify, deploy, hard-restart, accept, document, and publish Onyx Server in order."
