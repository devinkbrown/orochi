<!-- SPDX-FileCopyrightText: 2026 Devin Brown <devin.kyle.brown@gmail.com> -->
<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->

# TLS Hardening Roadmap

Derived from a gap analysis of the Onyx Server Zig-native TLS stack (`src/crypto/tls*.zig`,
`x509*.zig`, `ocsp.zig`, `sct.zig`, `crl.zig`) against BoringSSL and WolfSSL.

**Where we're already ahead — keep these:** live TLS session export/import across the
USR2 hot-upgrade (no re-handshake); aggressive modern-only posture (no CBC, no
static-RSA/DHE, no TLS <1.2, no SHA-1 cert sigs, renegotiation refused); PQ-hybrid
KEX (X25519MLKEM768) wired server-side; 0-RTT with a shared replay guard **plus a
RFC 8446 §8.2–8.3 `obfuscated_ticket_age` freshness window** (v3 sealed tickets
carry `ticket_age_add`; the server rejects age skew beyond
`early_data_age_skew_ms`); TLS 1.2 **Extended Master Secret (RFC 7627) required by
default** on both roles; RFC 5705/9266 exporters for SCRAM channel binding.

Effort: **S** ≤½ day · **M** 1–3 days · **L** multi-day/spike-first.
Risk: how dangerous a mistake is on the *live* stack.

---

## Phase 0 — Assurance foundation (a from-scratch TLS stack MUST have this)

| # | Item | Effort | Risk | Notes |
|---|------|--------|------|-------|
| 0.1 | ✅ **DONE (rejection subset)** — **Adversarial crypto tests** (`wycheproof_kat.zig`): AEAD tamper (ct/tag/nonce/aad), X25519 all-zero low-order point, ECDSA r=0/s=0. Follow-up: positive independent-vector KATs (RSA-PSS, full curve set). | M | Low | `wycheproof_kat.zig` |
| 0.2 | ✅ **DONE** — **Fuzz harnesses** (`tls_fuzz.zig`): deterministic random + structured + bit-flip robustness over x509/record/handshake parsers; ~44k inputs/run, no panics found. Follow-up: coverage-guided `--fuzz` (Smith). | M | Low | `tls_fuzz.zig` |
| 0.3 | ✅ **SHIM IMPLEMENTED (reachable subset; external runner is the out-of-repo CI piece)** — `tools/bogo_shim.zig` + `zig build bogo-shim` (build step, kept OUT of `zig build test`). A standalone tool speaking BoringSSL `ssl/test/runner`'s shim contract: dial `127.0.0.1:<-port>`, write the `-shim-id` LE prefix, drive Onyx Server's engines via the SAME public surfaces the daemon uses (`TlsConn` server / `tls_client.Client`, `tls_certs.loadOrBootstrap`), XOR-echo app data, and exit with the BoGo codes (**0** pass / **89** unimplemented / **1** error, synthesizing a fatal alert via the engine `takeAlert`/`alertRecordForError` first). **Touches NO `src/crypto`/`src/daemon` production code** — purely additive in `tools/` + `build.zig`. **Handled flags:** `-server`, `-port`, `-shim-id`, `-cert-file`, `-key-file`, `-min-version`, `-max-version`, `-expect-version`, `-curves` (client: 23→secp256r1, 4588→X25519MLKEM768), `-select-alpn`/`-expect-alpn` (server). **Cases:** TLS 1.3 both roles + hardened 1.2 server (auto-negotiated for a 1.2 ClientHello when the leaf key is ECDSA/RSA), version negotiation, curve constraint, server ALPN select/assert, malformed-handshake → alert → nonzero. **Out of scope ⇒ exit 89 (documented):** TLS<1.2, DTLS, resumption (`-resume-count`)/0-RTT, mTLS/`-verify-peer`, HRR, `-shim-writes-first`/`-shim-shuts-down`, client-role ALPN, non-{P256,hybrid} curves, and every other flag. **Self-driven proof (no external harness):** `zig build bogo-shim-test` — 17 tests (parse + record-framing units + 5 subprocess smokes that spawn the REAL binary and drive it with Onyx Server's own loopback engines, asserting exit 0 / 89 / nonzero + the XOR echo); the Debug binary's safety checks make a memory fault a hard test failure. **TODO (out-of-repo):** pin BoringSSL, `tools/bogo.sh` driver + `-shim-config` (DisabledTests/ErrorMap encoding the modern-only posture), and the CI `bogo` job (informational → required). **Latent bug surfaced (NOT fixed — production untouched):** `TlsConn.onInbound`'s `self.engine = .{ .tls12 = try tls12_server.Server.init(...) }` leaves the union in a corrupt half-constructed `.tls12` state if 1.2 init fails (`NoSigningKey`); the daemon never hits it (its `cfg12` is always keyed) and the shim now avoids it by running 1.3-only when the leaf can't do 1.2 — worth a surgical fix in a later pass. | L | Low | `tools/bogo_shim.zig`, `build.zig`; design `tls-design/bogo.md` |
| 0.4 | ✅ **DONE** — **Constant-time verification** (`zig build ct-check`): opt-in dudect-style Welch's-t harness over ECDSA-P256 sign, X25519 scalarmult, and RSA-2048 blinded private op. Kept out of `zig build test` because timing gates are inherently noisy; default ReleaseFast run gates at `|t| < 10` and prints per-primitive crop/mean evidence. RSA varies the input representative with a fixed key (the blinding property under test); ECDSA/X25519 vary the secret. | M | Low | `tools/constant_time_check.zig`, `build.zig` |

2026-07-08 closure: the `TlsConn.onInbound` union-init hazard noted in 0.3 is fixed. Both TLS 1.3 and TLS 1.2 engines now initialize into locals before assigning the `Engine` union, and a regression test proves a TLS 1.2 `NoSigningKey` init failure leaves the adapter in `.undecided` so alert generation and deinit remain safe.

## Phase 1 — Surgical correctness fixes ✅ COMPLETE (only 1.3 PSS-384/512 + x509_verify alignment as noted follow-ups)

| # | Item | Effort | Risk | Files |
|---|------|--------|------|-------|
| 1.1 | ✅ **DONE** — **Downgrade sentinel (RFC 8446 §4.1.3)**: 1.2 engine stamps `DOWNGRD\x01` into `server_random[24..32]` at construction (covers full + resumed ServerHello). Follow-up: have `tls12_client` check it. | S | Med | `tls12_server.zig` |
| 1.2 | ✅ **DONE** — **Path-length basicConstraints enforcement**: `x509` retains `basic_constraints_path_len: ?u32`; validator rejects chains exceeding a CA's pathLen via the pure unit-tested `enforcePathLen` helper. | S | Med | `x509.zig`, `tls_client.zig`, `x509_selfsign.zig` |
| 1.3 | ✅ **DONE (both client legs aligned)** — **The TLS 1.2 AND 1.3 client chain verifiers now delegate their signature primitive to the single, independently-tested `x509_verify.verifySignedBy`** (via `linkInfo`), inheriting the full sig-alg set: **RSA PKCS#1 SHA-256/384/512, RSASSA-PSS, ECDSA P-256/P-384, Ed25519** — real CA links signed with sha384WithRSA, RSASSA-PSS, or ecdsa-with-SHA384 anchor correctly. #46 aligned `tls12_client`; the 1.3 follow-up aligns `tls_client.zig` (`verifyChainToTrustAnchors`/`verifyIssuedBy`), removing its duplicated local `verifyCertSignature`/`verifyCertPss` + orphaned OID consts (no dead code) while **keeping `extractCertParts`** (still used by the DC/SCT/OCSP/CRL/path-len paths) and leaving the RPK/DC/SCT/ECH composition, leaf-key handling, and validity/EKU/basicConstraints logic untouched. To keep delegation a **strict superset with no regression**, `x509_verify` gained an **ecdsa-with-SHA384 / P-384** branch (parses the P-384 issuer point from the SPKI locally — the `x509.SubjectPublicKey` union deliberately does not model P-384, avoiding the `oauth_jwt` exhaustiveness blast radius); this also uplifts every other caller of the shared primitive — `tls12_client`, `verifySimpleChain`, and the CRL (`crl.zig`) + OCSP (`ocsp.zig`) signature checks — to accept P-384 (additive, fail-closed; a P-384 signature is unforgeable without the P-384 key, and each caller's own gates are untouched). `x509_selfsign` gained a `buildSelfSignedEcdsaP384` minter for the end-to-end matrix. | M | Med | `tls_client.zig`, `tls12_client.zig`, `x509_verify.zig`, `x509_selfsign.zig` |
| 1.4 | ✅ **DONE** — **Client offers X25519MLKEM768**: hybrid ephemeral + `ek‖x25519` key_share + raw-concat combiner (matching the server, NOT `HybridKx.decapsulate`); key schedule now takes a variable-length secret. Integration-tested (hybrid-only client ↔ server completes, group 0x11ec, data round-trips). | S–M | Med | `tls_client.zig`, `supported_groups.zig`, `tls_server.zig` |
| 1.5 | ✅ **DONE (TLS 1.3)** — **record_size_limit (RFC 8449)**: negotiated both ways (offer/echo/parse/validate) + outbound records fragmented in `encrypt()` to honor the peer's limit (`recordContentLimit`). Integration-tested (100-byte limit → 250B payload fragments to 3 records, client reassembles). Follow-up: TLS 1.2. | M | Med | `tls_extension.zig`, `tls_record.zig`, `tls_server.zig`, `tls_client.zig` |
| 1.6 | ✅ **DONE** — **Ticket-key rotation**: `openTicketWithRotation` + `previous_ticket_key` on both TLS Configs; daemon retains `tls_previous_ticket_key`, threads it in, and rotates on REHASH (`rotateTicketKey`). Unit-tested both layers. Minor follow-up: carry `previous` across USR2. | M | Med | `tls_resumption.zig`, `tls12_server.zig`, `tls_server.zig`, `daemon/server.zig` |

## Phase 2 — Server-facing protocol features ✅ COMPLETE (2.1 OCSP stapling · 2.2 HRR · 2.3 cert compression · 2.4 SNI selection)

| # | Item | Effort | Risk | Notes |
|---|------|--------|------|-------|
| 2.1 | ✅ **COMPLETE** — server-side OCSP stapling, wire + producer. **Wire (inc 1-5):** ocsp.buildRequest[ForCerts]; x509 AIA-URL + must-staple + CertID inputs; TLS 1.3 CertificateEntry staple; TLS 1.2 CertificateStatus (gated, mirrors 1.3). Both legs staple `Config.ocsp_staple` only when the client offers status_request (byte-identical otherwise). **Gate (inc 6):** `ocsp.isStapleServable` (parse→successful→issuer-signed→good→thisUpdate≤now<nextUpdate±skew, throw-free), `x509.generalizedTimeToEpoch`, `http_fetch.post`. **Publish (inc 7):** `config.tls_ocsp_staple` + `publishOcspStaple`/`maybeSwapOcspStaple` reactor-0 atomic-swap (mirrors ACME reload); 1.2 stapled only when it shares the 1.3 leaf; staple cleared on cert reload. **Producer (inc 8):** `ocsp_staple.Service` background thread — fetch AIA → verify → freshness-gate → cache last-good → publish; revoked-own-cert logs CRITICAL; refresh at thisUpdate→nextUpdate halfway clamped [5m,24h]. `[ocsp]` config + main.zig wiring (gated on `[tls]` + on-disk cert_path). **Follow-up (low pri):** 1.2 client-side round-trip test; delegated-responder (id-kp-OCSPSigning) support; exponential backoff on fetch failure. | M | Med | `ocsp.zig`, `x509.zig`, `tls_server.zig`, `tls12_server.zig`, `tls_extension.zig`, `ocsp_staple.zig`, `http_fetch.zig`, `server.zig`, `config_format.zig`, `main.zig` |
| 2.2 | ✅ **COMPLETE** — server-side HelloRetryRequest (RFC 8446 §4.1.4 + §4.4.1). When a ClientHello offers no key_share the server can use but advertises a group it supports (via `supported_groups`), the server sends one HRR — a ServerHello with the magic random + a bare-group key_share — reusing `supported_groups.selectPreferred` (prefs x25519 › secp256r1 › x25519mlkem768). Transcript rewritten with the `message_hash` synthetic (type 254) before folding in the HRR; new `wait_second_client_hello` state; ClientHello2 is pinned to exactly the requested group (extra/other shares dropped, else `BadHandshake`); only one HRR per connection; 0-RTT skipped on the bounced hello. The in-repo `tls_client` HRR path was made real+compliant (single-share CH2 via `retry_key_share_group`, offered-share-aware rejection) and drives a full loopback interop test (withheld shares → HRR → retry → data both ways). Wire byte-identical for normal handshakes (any usable share ⇒ no HRR). | M | Med | `tls_server.zig`, `tls_client.zig`, `supported_groups.zig` |
| 2.3 | ✅ **COMPLETE** — certificate compression (RFC 8879), zlib, both directions. Shared `proto/cert_compression.zig` (Algorithm registry, `pickSupported`, `deflateZlib`, `inflateZlib` with the mandatory 3-layer bomb guard: declared-len ≤ 64 KiB before alloc, output capped at declared+1, exact-length match) built on the in-tree `std.compress.flate` `.zlib` container — **no new dependency**. Server: `compress_certificate(27)` negotiation, `Config.enable_cert_compression` (default off ⇒ byte-identical), `writeCertificate` emits `CompressedCertificate(25)` only when it actually shrinks (fallback to plain). Client: opt-in `offer_cert_compression` (default off, symmetric with the server gate) advertises zlib + decodes type-25 under the bomb guard (`decompressCertificate`), and rejects an unsolicited CompressedCertificate. zlib only — std has no brotli and its zstd is decode-only; brotli/zstd-only clients fall back to plain (no interop break). TLS 1.3 only. Tests: compressed round-trip loopback (duplicated chain forces compression) + module unit/bomb tests + a deterministic fallback-premise unit test (real self-signed certs always deflate smaller, so the writeCertificate fallback branch is proven via `deflateZlib` expanding tiny incompressible input rather than a fragile cert-based loopback). | M | Low | `cert_compression.zig`, `tls_extension.zig`, `tls_server.zig`, `tls_client.zig` |
| 2.4 | ✅ **COMPLETE (library)** — SNI-based cert selection (RFC 6066). `Config.sni_certs: []const SniCert` ({server_names, cert_chain, signing keys, ocsp_staple}); on ClientHello the server parses SNI via the existing `proto/sni.zig` (`extractOptional(raw)`) and picks the first entry whose `server_names` matches (case-insensitive exact + `*.` one-label wildcard, `serverNameMatches`), else the default top-level cert. `writeCertificate`/`writeCertificateVerify` route through `activeCertChain`/`activeOcspStaple`/`activeSigningKeyResolved` accessors; `sni_cert` index re-derived per ClientHello; `init` validates every entry has a chain + key. Empty `sni_certs` ⇒ byte-identical (SNI not consulted). Loopback test: 2 SNI certs + default, three connections select A/B/default (a green handshake against each cert's SAN+anchor *is* the selection proof) + `serverNameMatches` unit test. **Daemon plumbing (multi-cert `[tls]` config + main.zig load) is a separate follow-up** — this is the TLS-library capability, the ECH (5.1) prereq. | M | Low | `tls_server.zig`, `sni.zig` |

## Phase 3 — Performance

| # | Item | Effort | Risk | Notes |
|---|------|--------|------|-------|
| 3.1 | ✅ **TX+RX offload COMPLETE (gated `[tls] ktls=tx`/`txrx`, TLS 1.3, USR2-safe)** — see [design](tls-design/ktls.md) | M–L | Med | **RX (this session):** `Server.ktlsRxParams` + `attachRx` + `TlsConn.enableKtlsRx`/`hasBufferedInbound`; `driveTls` attaches RX one-shot at the connected transition when the inbound is at a clean record boundary; `driveClientBytes` routes an RX-offloaded conn's (kernel-decrypted) plaintext straight to `feedBytes`/`driveWs`; a **control record surfaces as a recv error → conn closes** (experimentally confirmed: plain `recv()` on a kernel-decrypted client KeyUpdate returns an error, never app-data); USR2 `rx_offloaded` marker (path-A, kernel RX state survives execve on the carried fd). Integration test PROVES it (client encrypts → kernel decrypts → `recv()` plaintext). RX attach stays userspace if a partial inbound record is buffered. **TX (earlier this session):** `ConnState.tls_tx_offloaded`/`ktls_attach_pending`; `driveTls` marks a completed TLS-1.3 conn pending when `[tls] ktls` is on + kernel-capable, and `maybeAttachKtlsTx` calls `TlsConn.enableKtlsTx(fd)` once the send buffer (inline + SendQ overflow) fully drains at the connected transition / send-completion; `appendSecuredToConn` hands the kernel plaintext for offloaded conns (kernel encrypts on `send()`); a post-handshake TLS record on an offloaded conn drops it (no KeyUpdate/NST double-encrypt). USR2: a 1-bit `tx_offloaded` marker rides the `tls_snapshot` (the inherited-fd kernel TX state survives execve, so the successor re-attaches nothing — just resumes sending plaintext; `pending_out` carried as plaintext). **Proven end-to-end: the kernel encrypts a server write with our `crypto_info` and the in-repo `tls_client` decrypts it.** Default off ⇒ byte-identical. **Phase 0 (earlier this session):** kernel-UAPI constants (`SOL_TLS`/`TCP_ULP`/`TLS_TX`/versions), per-cipher `crypto_info` geometry (AES-GCM-128/256, ChaCha20-Poly1305), the `CryptoInfo.encode` byte serializer (authoritative UAPI layout: header ‖ iv ‖ key ‖ salt ‖ rec_seq), big-endian `seqToBytes`, and the TLS 1.3 static-IV → salt(4)/iv(8) (GCM) / no-salt+12-byte-iv (ChaCha) split, all golden-vector unit-tested. **Deferred to Phase 1 (needs deploy-kernel round-trip):** TLS 1.2 explicit-nonce derivation, the `setsockopt(TCP_ULP/TLS_TX)` attach + send-seam plaintext branch, boot per-suite capability probe, getsockopt-seq liveness self-test, `[tls] ktls=off\|tx\|txrx` config, USR2 offloaded-marker. Biggest throughput/CPU win. **Design correction (wave 1):** kTLS state is *kernel socket state that survives `execve`*, and we already carry client fds across USR2 — so a TX-offloaded socket keeps encrypting on the successor with **zero key re-injection** (add a 1-bit "offloaded" marker to the snapshot). The USR2 collision I'd assumed largely dissolves. **S2S is NOT TLS** (Mooring/Undertow) so it's out of scope — the only eligible sockets are client-facing implicit-TLS + wss. Recommended: **TX-only first** (RX kept userspace; RX extraction is fragile), rolled out to the wss media/upload data-plane before interactive IRC clients. |

## Phase 4 — Wire-or-cut (verifier code exists but is unreachable)

| # | Item | Effort | Risk | Decision |
|---|------|--------|------|----------|
| 4.1 | ✅ **DONE (wired, opt-in) + SCT presence-enforcement** — embedded-SCT (Certificate Transparency) verification is wired into the TLS 1.3 client. New `x509.buildPrecertTbs` reconstructs the precertificate TBSCertificate (leaf TBS with the SCT-list extension `1.3.6.1.4.1.11129.2.4.2` removed, all enclosing lengths re-encoded to canonical minimal DER — exact-byte, tested across the long-form boundary); `tls_client` consumes it via config-gated `Options.ct_logs` (caller-supplied pinned `sct.CtLog` set, deep-copied). **Two composable, opt-in policies:** (a) `enforce_sct` = tamper-detection — a pinned-log-matched + signature-INVALID SCT `error.BadSct`; (b) **`require_sct: u8` = presence + distinct-log quorum** — the leaf MUST present ≥ N SCTs that each verify against a **DISTINCT pinned log** (RFC 6962 §5.1-style; duplicate SCTs from one log count once), else `error.InsufficientScts`. `require_sct` closes the mis-issuance gap `enforce_sct` cannot: an attacker embedding NO SCT (or only unpinned-log SCTs) previously bypassed enforcement; now every fail-open path yields a zero summary that falls below the threshold and rejects. `sct.ListSummary` gained `distinct_valid_logs` (the quorum count). OPT-IN + byte-identical-when-off: empty `ct_logs` OR `require_sct == 0` (default) ⇒ path fail-open exactly as before. Handles the CA-signs-precert-directly case (Precertificate Signing Certificates out of scope → fail-open unless `require_sct` set). | M | Low | `tls_client.zig`, `x509.zig`, `sct.zig` |
| 4.2 | ✅ **DONE (wired, fail-open)** — `crl.zig` parses + self-verifies the CRL's outer signature against the leaf issuer's key; `tls_client` now checks leaf revocation via config-gated `Options.crl` (caller-supplied CRL DER, e.g. from a CDP fetch). FAIL-OPEN: only an authentic + current CRL that lists the serial can `error.CertificateRevoked`; missing/broken/wrong-issuer/stale/no-clock is ignored. zig-review **Approve**. Follow-up (low): in-daemon CDP fetch + cache. | M | Low | `tls_client.zig`, `crl.zig` |
| 4.3 | **DTLS** — full DTLS 1.2/1.3 + DTLS-SRTP lib, no live listener; CID absent | — | Low | Keep as media-plane lib or cut; only relevant if media → standards WebRTC |

## Phase 5 — Deferred / forward-looking (revisit in 12–18 months)

| # | Item | Notes |
|---|------|-------|
| 5.1 | 🟢 **CLIENT + SERVER acceptance DONE (config-supplied ECHConfig)** — `ech_config.zig` parses the ECHConfigList (`0xfe0d`) fail-closed + selects a supported entry; `ech_seal.zig` builds the HPKE `info`/padding/seal (reuses `hpke.zig`: X25519/HKDF-SHA256/ChaCha20-Poly1305) + the outer extension + the §7.2 acceptance confirmation. **CLIENT** (`tls_client`) splits the ClientHello into an HPKE-sealed inner (real SNI) inside an outer (SNI = `public_name`) when `Options.ech_config_list` supplies a usable config, checks the ServerHello accept signal and switches to the inner transcript, else falls back to the outer/`public_name`. **SERVER** (`tls_server`) accepts `Config.ech_keys` (each = a single-entry ECHConfigList + its X25519 private key, validated at `init`); on a ClientHelloOuter carrying an openable `encrypted_client_hello`, it HPKE-**opens** the inner (AAD = the header-less outer body with the payload zeroed), reconstructs the ClientHelloInner (restores the outer's `legacy_session_id`, strips §6.1.3 padding — byte-identical to the client's retained inner), runs the whole rest of the handshake over it, and stamps the §7.2 confirmation into `ServerHello.random[24..32]`. Proven by an onyx-client↔onyx-server loopback (server opens the inner, client's accept-confirmation passes, data round-trips). Default off (empty `ech_keys`) / no-ECH / unknown config_id / open failure ⇒ **fall back to the outer, never abort** ⇒ **byte-identical**. **`retry_configs` (§7.1) DONE:** when ECH keys are configured and this handshake did NOT accept ECH, the server emits the published `ECHConfigList` (`Config.ech_retry_config_list`) as `retry_configs` in EncryptedExtensions so the client can refresh its config — gated on the `ech_offered` latch (set only when a client actually offered ECH we could not open), so a plain non-ECH client NEVER draws an unsolicited `retry_configs` and the wire stays byte-identical. **Daemon plumbing:** `[tls]` now accepts `[[tls.ech_keys]]` with `config_path` (single-entry ECHConfigList file) plus 64-hex-byte `private_key`; `main.zig` loads the config bytes, validates them through `tls_server.Server.init` before wiring the listener, and passes them to the live TLS engine. **Deferred follow-ups:** DNS HTTPS-RR fetch (config-supplied for now, mirroring CRL/SCT); `ech_outer_extensions` decompression (server stands down to the outer if a client compresses — the Onyx Server client sends the full inner ext set); ECH-over-PSK/0-RTT and ECH+HRR (both stand down to the outer/plain today — see the note below, being revisited in a parallel change); non-X25519 HPKE suites; AAD header-inclusion confirmation against a third-party reference ECH server before live interop. | On-brand for privacy; DNS HTTPS-RR publication/fetch remains |
| 5.2 | Delegated credentials (RFC 9345) | ✅ **CLIENT + SERVER DONE** (opt-in, default-off, byte-identical when off). **Client:** offers `delegated_credential` (34) as a SignatureSchemeList; on a server-presented DC in the leaf `CertificateEntry` it enforces the leaf's `id-ce-delegationUsage` (+digitalSignature KeyUsage), verifies the DC signature under the **leaf key** over the RFC 9345 §4.1.3 content, enforces the window (`notBefore + valid_time`, 7-day max, clock required), then binds CertificateVerify to the **DC key** (scheme must equal `dc_cert_verify_algorithm`). **Server:** `tls_server.Config.delegated_credential` = a pre-minted `{wire, private_key}`; presented in the leaf `CertificateEntry` **only** when the client offers the extension AND accepts the DC's `dc_cert_verify_algorithm` AND no SNI cert was selected (the DC binds `cert_chain[0]`), and CertificateVerify is then signed with the **DC private key**. `Server.init` fail-fasts a DC whose wire doesn't parse, whose key kind ≠ scheme, or whose leaf-signature algorithm ≠ the default leaf key. New pure `proto/delegated_credential.zig` codec; `x509` parses DelegationUsage; `x509_selfsign` grew DelegationUsage/KeyUsage test emitters. **Tooling:** `onyx delegated-credential inspect|validate` parses ready-made DC wire bytes and validates leaf signature/DelegationUsage/KeyUsage/lifetime against a leaf cert. **Follow-up:** DC **mint/rotation tooling** (production private-key loading + DC key-output policy are still intentionally not added); client DCs for mTLS. |
| 5.3 | Raw public keys (RFC 7250) | ✅ **CLIENT + SERVER DONE, including mTLS client raw public keys** — client: `tls_client` opt-in (`Options.offer_raw_public_key`, default off ⇒ ClientHello byte-identical) offers `server_certificate_type=[RawPublicKey,X509]`, consumes the EE selection, and parses a bare-SPKI Certificate into the handshake-signature key (reusing `parsePublicKeyFromSpki`, no new crypto). Server: `tls_server` opt-in (`Config.enable_raw_public_key`, default off ⇒ EncryptedExtensions + Certificate byte-identical); when a client offers `server_certificate_type` including RawPublicKey and the server is configured for it, the server selects RawPublicKey, echoes the single-value selection in EncryptedExtensions, and sends a Certificate whose one CertificateEntry `cert_data` is the active leaf's bare `SubjectPublicKeyInfo` (`x509.parse(leaf).spki_der`, no chain, no OCSP staple) — CertificateVerify is still signed with the same leaf key (Ed25519/ECDSA-P256/RSA). For mTLS, when `request_client_cert` and raw public keys are both enabled, the server parses the client's `client_certificate_type` offer, selects RawPublicKey in CertificateRequest, accepts a bare client SPKI CertificateEntry, and verifies CertificateVerify against that SPKI before exposing it as the CertFP identity bytes. Real cross-Onyx Server loopback proof covers both directions: server bare-SPKI auth data round-trips, and raw-client-SPKI mTLS completes with the server exposing exactly the presented SPKI. RawPublicKey NEVER bypasses X.509 trust — it's a distinct, opt-in, no-CA (TOFU/pinning) path. **Daemon/TOML wiring exists as `[tls] raw_public_key` (default false), composing with `[tls] request_client_cert` for client raw-key mTLS.** Mesh still uses Mooring. |
| 5.4 | PQ-signature certs (ML-DSA/SLH-DSA) | 🟢 **ML-DSA-44/65/87 + ALL 12 SLH-DSA parameter sets VERIFY DONE — every set KAT-verified against independent NIST ACVP vectors, wired into X.509 dispatch.** From-scratch `crypto/ml_dsa.zig` implements FIPS 204 `ML-DSA.Verify`/`Verify_internal` generic over a comptime `Params` struct (44/65/87), verify-only (Onyx Server verifies PQ certs, never signs them): plain `% q` NTT (comptime ζ=1753 table shared across sets; no Montgomery — public data, no CT requirement), pkDecode/sigDecode (HintBitUnpack ⊥ fail-closed; z-width/hint-len per set), ExpandA (SHAKE128), SampleInBall (SHAKE256), Decompose/UseHint/w1Encode, ‖z‖∞<γ1−β + c̃′==c̃. **Pinned per set by INDEPENDENT NIST ACVP `ML-DSA-sigVer` KATs** (`usnistgov/ACVP-Server`): 65 (`ml_dsa_kat.zig`) tgId 3+10 incl. empty-context tcId 35; 44 (tgId 1+8) + 87 (tgId 5+12) in `ml_dsa_variants_kat.zig`; 30 cases/set covering accept + all four reject classes — NOT self-generated. **SLH-DSA:** from-scratch `crypto/slh_dsa.zig` implements FIPS 205 `slh_verify`/`slh_verify_internal` as a single comptime-generic `Verifier(P)` instantiated for **all 12 standardized parameter sets** — SHA2 and SHAKE, {128,192,256}×{s,f} (verify-only): full WOTS+ / FORS / XMSS / hypertree recomposition with the correct ADRS/tweakable-hash family per set — **SHAKE** (F/H/T_ℓ = SHAKE256 over the FULL 32-byte ADRS; H_msg = SHAKE256), **SHA2 cat 1** (F/H/T_ℓ = Trunc_n(SHA-256(PK.seed‖0‖ADRS^c‖M)) with the 22-byte compressed ADRS + precomputed midstate; H_msg = MGF1-SHA-256), and **SHA2 cat 3/5** (F stays SHA-256 but H/T_ℓ use SHA-512 with a 128-byte-block prefix; H_msg = MGF1-SHA-512) — MSB-first base_2^b, the full-64-bit `idx_tree` mask edge case (…-256f), fail-closed length checks. **Correctness pinned by INDEPENDENT NIST ACVP FIPS 205 `SLH-DSA-sigVer` KATs for every one of the 12 sets** — external/pure + internal accepts and real "modified message"/"modified signature" reject verdicts, plus one empty-context accept per hash family from the matching `SLH-DSA-sigGen` corpus (pk=sk[2n:4n]) exercising the X.509 empty-context path; the KAT asserts a *passing* accept for each of the 12 sets and adds dynamic bit-flip tamper coverage — NOT self-generated. `x509.extractMlDsa{44,65,87}PublicKey` (exact-OID `2.16.840.1.101.3.4.3.{17,18,19}`, absent params, exact 1312/1952/2592-byte key) + three ML-DSA dispatch arms, and `x509.extractSlhDsaPublicKey` (exact id-slh-dsa-* OID `2.16.840.1.101.3.4.3.{20..31}`, absent params, exact 2n-byte key) + a `verifyCertSignature` dispatch table over the 12 SLH OIDs (empty context per draft-ietf-lamps-x509-slhdsa, cross-checked against the IETF LAMPS drafts + OpenSSL). Both PQ families isolated from the classical `SubjectPublicKey` union (own entry points ⇒ no churn to ~10 unrelated verifiers); byte-identical when off (no cert carries these OIDs). **Not yet:** PQ *signing*; hybrid/composite certs; live-interop (no PQ CA issues yet). | S–M | Low | `crypto/ml_dsa.zig`, `crypto/ml_dsa_kat.zig`, `crypto/ml_dsa_variants_kat.zig`, `crypto/slh_dsa.zig`, `crypto/slh_dsa_kat.zig`, `crypto/x509.zig`, `crypto/x509_verify.zig` |

## Explicitly skipped (cut against modern-only posture)

AES-CCM/CCM_8 · X448/Ed448 · P-521 · DHE · static-RSA · CBC/encrypt-then-MAC ·
extended master secret (1.2 tickets off by default) · DTLS Connection ID (unless media pivots).

---

Design docs (wave 1): [0.3 BoGo](tls-design/bogo.md) · [2.1 server OCSP stapling](tls-design/ocsp-stapling.md) · [2.3 cert compression](tls-design/cert-compression.md) · [3.1 kTLS](tls-design/ktls.md).

## Wave 1 outcomes (2026-07-05)

20-agent workflow: 4 design spikes + 8 worktree-isolated implementations + 8 adversarial reviews.

**Designs — all 4 usable**, landed in `docs/dev/tls-design/`. Key correction: the kTLS↔USR2-migration tension mostly dissolves (kernel TLS state survives execve).

**Implementations — process learning:** the worktree isolation branched from a **stale base (`50ca052`, 673 commits behind HEAD)**, so most patches don't apply to HEAD and the agents' "compiles: yes" was against old code. Adversarial review + `git apply --check` triage:

| Item | Applies to HEAD | Review | Action |
|------|-----------------|--------|--------|
| 1.1 downgrade sentinel | ✅ clean | approve | **LANDED** |
| 1.3 validator consolidation | ✅ clean | revise (CRITICAL: `oauth_jwt.zig` union-exhaustiveness build break; 3rd validator in `tls12_client` left) | redo vs HEAD |
| 1.6 ticket-key rotation | ✅ clean | revise (dead code — `TicketKeyRing` unwired; no secureZero) | wire it, then land |
| 0.1 Wycheproof KATs | ✗ conflict | revise (self-generated "known" answers ≠ independent; mislabeled GCM vector) | redo with real vectors |
| 0.2 fuzz harnesses | ✗ conflict | revise (misses 2nd record parser; stale-base provenance) | redo vs HEAD |
| 1.2 path-length | ✗ conflict | revise (stale hunks; 3-arg call mismatch) | redo vs HEAD |
| 1.4 client PQ offer | ✗ conflict | revise (only consumer is ACME outbound; CH fragmentation risk) | redo, assess value |
| 1.5 record_size_limit | ✗ conflict | revise (stale hunks; missing upper-bound + alert) | redo vs HEAD |

**Wave-2 fix:** run implementers against **HEAD** (not a stale worktree base), and treat these reviewed patches as reference specs. The reviews already pinpoint each fix.

## Execution log

- 2026-07-11 (v0.4.0): **TLS 1.2 Extended Master Secret required by default (RFC 7627).** Both the hardened TLS 1.2 server and client set `require_extended_master_secret = true`: a ServerHello that fails to echo the empty `extended_master_secret` extension aborts with `EmsRequired`, and the master secret derives from the session hash (`tls12.emsMasterSecret`) rather than the raw randoms. Resumption tickets carry an `EM12` tag so a resumed session proves the original handshake was EMS. The flag may be relaxed only for legacy interop; even then EMS is negotiated and used whenever the peer offers it. (`tls12_server.zig:98`, `tls12_client.zig:89`, `tls12.zig:297`.)
- 2026-07-11 (v0.4.0): **0-RTT `obfuscated_ticket_age` freshness window (RFC 8446 §8.2–8.3).** The sealed-ticket format bumped to v3, adding `ticket_age_add` (`tls_resumption.zig`); on a 0-RTT attempt the server un-obfuscates the client's `obfuscated_ticket_age` and rejects any skew beyond `early_data_age_skew_ms` (default 10 s) BEFORE consuming a binder-ring slot — a temporal defense the single-process replay ring cannot provide. Legacy v1/v2 tickets carry no `age_add` and gracefully degrade to the binder-ring-only check. 0-RTT is opt-in via `[tls] early_data_max_size` (default 0). (`tls_server.zig:186`, `tls_server.zig:1801`, `tls_resumption.zig:25`.)
- 2026-07-11 (v0.4.0): **TLS 1.3 HelloRetryRequest pinning.** ClientHello2 is now pinned to the HRR commitments: cipher-suite change vs `hrr_suite` → `BadHandshake`; `key_share` must supply exactly `hrr_group` (other-group shares dropped, none remaining is fatal); `early_data` after HRR is rejected; still only one HRR per connection. A record-layer fault in the `connected` (post-handshake) state fails closed — `takeAlert` returns null and the caller closes the connection bare. (`tls_server.zig:1665`, `tls_server.zig:1672`, `tls_server.zig:1675`, `tls_server.zig:754`.)
- 2026-07-11 (v0.4.0): **ECH `retry_configs` (draft-ietf-tls-esni §7.1).** On ECH-not-accepted the server returns the published `ECHConfigList` as `retry_configs` in EncryptedExtensions, gated on the `ech_offered` latch so a non-ECH client never draws an unsolicited list (wire byte-identical). ECH acceptance is still confirmed only on a single-round ServerHello. **Status flag:** `signature_algorithms_cert` (RFC 8446 §4.2.3, type 50) is IMPLEMENTED — opt-in client emit + server chain-cert enforcement gated behind `enforce_cert_signature_algorithms` (OFF by default ⇒ byte-identical when off), fail-closed. A former note here claimed a 7,910-case external BoGo pass; the pinned runner's current reproducible evidence is only the exact 24-pass/3-skip required baseline, while the exploratory full corpus still has failures. ECH+HRR remains a deliberate fail-closed refusal (documented follow-up — the ECH-draft HRR confirmation transcript is not implemented; the client refuses rather than emit a subtly non-compliant CH2). (`tls_server.zig:2018`, `tls_server.zig:1911`, `tls_server.zig:620`.)
- 2026-07-06: **5.1 Encrypted Client Hello — SERVER-SIDE acceptance landed (opt-in, default-off, byte-identical when off).** `tls_server` now decrypts a ClientHelloInner from a client's ECH and signals acceptance, reusing the client-side crypto trio (`hpke.zig` + `crypto/ech_seal.zig` + `proto/ech_config.zig`) — no reimplementation. **Config:** `Config.ech_keys: []const EchKey` where `EchKey = { config: single-entry ECHConfigList, private_key: [32]u8 }`; `init` fail-fast-validates each (one `0xfe0d` entry, our KEM/suite, and `X25519.recoverPublicKey(private) == config.public_key`) → `error.BadEchConfig`. **Open path:** on ClientHello1, `maybeOpenEch` locates the outer `encrypted_client_hello`, reconstructs the **ClientHelloOuterAAD** (a dup of the header-less outer body with the payload region zeroed in place — matching the client's `buildOuterEchBody` convention), builds `info = "tls ech"‖0x00‖ECHConfig` from the config's retained `raw`, and HPKE-**opens** with the matching private key (trial-decrypt across config_id collisions; the AEAD tag is the gate). **Reconstruction:** `reconstructInnerHello` restores the outer's `legacy_session_id` into the decrypted EncodedClientHelloInner (whose inner session_id is sent empty, §5.1) and strips the §6.1.3 zero padding, producing a ClientHelloInner **byte-identical** to the one the client retained. The whole rest of the flight — transcript, negotiation, ServerHello — then runs over the inner (real SNI). **Accept signal:** `writeServerHello` builds the ServerHelloECHConf form (`random[24..32]` zeroed) and `stampEchConfirmation` overwrites those 8 bytes with `HKDF-Expand-Label(HKDF-Extract(0, inner.random), "ech accept confirmation", Transcript-Hash(ClientHelloInner‖ServerHelloECHConf), 8)` (§7.2) — the counterpart to the client's `checkEchAcceptance`. **FAIL-SAFE:** no-ECH / ECH off (empty `ech_keys`) / unknown config_id / AEAD open failure / malformed inner ⇒ null ⇒ the handshake proceeds over the ClientHelloOuter (public_name) and NEVER aborts; only genuine OOM propagates. **No secret-dependent branch on the ECH key** (config_id is cleartext; the trial-decrypt count is public; the branch is on the AEAD authentication outcome). **Deferred (each stands down to the outer/plain):** ECH+HRR (only CH1 is tried), ECH-over-PSK, `ech_outer_extensions` decompression, non-X25519 suites; plus DNS HTTPS-RR fetch and daemon `[tls]` plumbing. **Tests:** onyx-client (`ech_config_list`) ↔ onyx-server (matching `ech_keys`) loopback — server opens the inner, `server.ech_accepted`, `client.echAccepted()==true`, data round-trips both ways; a no-ECH ClientHello against an ECH-configured server takes the normal path (`!ech_accepted`, byte-identical); an unknown-config_id ECH offer falls back to the outer without abort (`client.echAccepted()==false`, handshake completes); `init` rejects a mismatched private key and a truncated list (`BadEchConfig`); a `locateOuterEch` unit test (parses a valid outer ext with exact payload offset, returns null on absent + truncated). Full unfiltered suite green (6240→6245 pass / 4 skip / 0 fail); `zig fmt` clean. zig-code-reviewer: <verdict pending>. **not deployed** (needs daemon plumbing + third-party reference-server interop before live). Base-tripwire honored (reset the stale `50ca052` worktree base to `d281594` before implementing).
- 2026-07-05: Roadmap created from the three-agent gap-analysis inventory.
- 2026-07-06: **5.2 Delegated Credentials (RFC 9345) — SERVER SIDE presentation landed (opt-in, default-off, byte-identical when off).** `tls_server.Config` gained `delegated_credential: ?DelegatedCredential` = a pre-minted `{ wire, private_key }` (the caller mints `wire` offline — the default leaf key signs the RFC 9345 §4.1.3 content — and holds `private_key`; both are BORROWED/caller-owned with the same lifetime contract as `cert_chain`/`signing_key`, never re-signed or zeroed by the engine). `Server.init` fail-fasts (invariant e) a DC whose `wire` does not parse (`BadDelegatedCredential`), whose `private_key` kind ≠ its `dc_cert_verify_algorithm` (`LeafKeyMismatch`), or whose leaf-signature `algorithm` ≠ the default leaf key's scheme (`BadDelegatedCredential`), caching the scheme in `dc_scheme`. The ClientHello parser sets `client_accepts_dc` when the client's `delegated_credential` SignatureSchemeList `offers` our DC's scheme (re-derived per ClientHello; a malformed list offers nothing → fail-closed). `activeDelegatedCredential()` presents the DC **only** when configured AND `client_accepts_dc` AND **no SNI cert was selected** (the DC binds `cert_chain[0]`); `writeCertificate` frames it into the leaf `CertificateEntry` extensions (alongside any OCSP staple) and `writeCertificateVerify` signs with the **DC private key** at the DC's scheme. Off ⇒ the leaf entry + CertVerify are byte-identical to a non-DC server. Tests: loopback — the 5.2 client offers, the server presents, the client verifies the DC under the leaf key and CertVerify under the DC key, handshake completes + app data; a non-offering client against the same DC-configured server completes on the leaf-key path (`dc_verified_key == null`); structural — leaf entry carries ext 34 == `wire` only when accepted, CertVerify scheme flips leaf→DC; **byte-identical-when-off** proven by comparing the DC-configured-but-not-accepted server's Certificate + (deterministic Ed25519) CertificateVerify against a no-DC server; SNI suppresses the DC; init rejects malformed/kind-mismatched/algorithm-mismatched configs. Full suite green. **Follow-up:** DC **issuance/rotation tooling** (nothing mints/rotates a DC yet); client DCs for mTLS. **not deployed.**
- 2026-07-06: **5.2 Delegated Credentials (RFC 9345) — CLIENT SIDE landed (opt-in, default-off, byte-identical when off).** New pure `proto/delegated_credential.zig` codec (parse/serialize the `DelegatedCredential` wire struct + reconstruct the RFC 9345 §4.1.3 leaf-signed content byte-identically by capturing the contiguous `Credential‖algorithm` slice, never re-serializing). `tls_extension` gained `delegated_credential = 34`; the client offers it as a SignatureSchemeList (`ecdsa_secp256r1/secp384r1`, `ed25519`, `rsa_pss_rsae_sha256`) only under the new `offerDelegatedCredentials()` opt-in — ClientHello byte-identical otherwise. On a server DC in the leaf `CertificateEntry` (captured only when offered), the client (a) requires the leaf's **`id-ce-delegationUsage`** (new `x509` parse + `delegation_usage` field) + digitalSignature KeyUsage, (b) verifies the DC signature under the **leaf certificate's key** over the §4.1.3 content using `DelegatedCredential.algorithm`, (c) enforces the window — `now ≤ notBefore + valid_time`, `valid_time ≤ 7 days`, **clock required** (no clock ⇒ reject), then (d) binds CertificateVerify to the **DC public key** (RSA key material copied out of the consumed handshake buffer, mirroring the leaf-key copy) and requires `CertVerify.algorithm == dc_cert_verify_algorithm`. Fail-closed on every defect. `x509_selfsign` grew DelegationUsage/digitalSignature-KeyUsage test emitters. Tests: codec round-trip + framing negatives; a valid DC accepted with CertVerify verifying under the DC key (and REJECTED under the leaf key — proves the substitution); isolated negatives for missing DelegationUsage, tampered DC signature, expired, over-7-day, no-clock, and CertVerify scheme-mismatch; ClientHello omits/carries-34 proof. Full suite green (6161→). **Follow-up:** server-side DC presentation + DC issuance/rotation (a server never sends a DC yet); client DCs for mTLS. **not deployed.**
- 2026-07-05: Wave 1 — 4 designs landed; 1.1 downgrade sentinel LANDED; 7 impls triaged to wave 2 (stale-base + review findings above).
- 2026-07-05: Wave 2 (re-implemented vs HEAD, not stale worktrees) — landed **6 items**: 1.1 downgrade sentinel, 1.2 path-length, 1.3 (partial) RSA-SHA384/512 cert verify, 1.6 ticket-rotation mechanism, 0.2 fuzz harnesses, plus the roadmap + 4 design docs. All compile clean, tested, committed; **none deployed** (held for a full-suite + zig-review gate).
  - **Surgical tier exhausted** for the cleanly-landable items. **Remaining, by size:**
    - Small follow-ups: 1.3 PSS-384/512 (needs PSS-param parsing) + align `x509_verify`; ~~1.6 daemon rotation trigger~~ **DONE** (rotate-on-REHASH + USR2 `previous`-carry both landed); 1.4 client-PQ (low value); 0.1 adversarial crypto KATs.
    - **Multi-file / larger:** 1.5 record_size_limit.
    - **Multi-day features (scoped by the design docs, need dedicated sessions):** 3.1 kTLS, 4.x wire-or-cut SCT/CRL/DTLS. (2.2 server HRR + 2.3 cert compression + 2.4 SNI selection now DONE — Phase 2 COMPLETE.)
- 2026-07-06: **5.1 Encrypted Client Hello — CLIENT side, config-supplied ECHConfig (PARTIAL, first substantial cut).** Two new pure modules + `tls_client` wiring. **`proto/ech_config.zig`:** fail-closed, zero-alloc ECHConfigList parser (`0xfe0d`) — bounds-checks every length, iterates entries, parses `HpkeKeyConfig` (config_id/kem_id/public_key/cipher_suites) + `maximum_name_length` + `public_name` + extensions; `selectSupported` picks the first entry with our KEM+suite, a valid LDH `public_name`, and no unsupported-mandatory ECHConfig extension (skips unknown-version entries). **`crypto/ech_seal.zig`:** reuses `hpke.zig` (DHKEM-X25519 / HKDF-SHA256 / ChaCha20-Poly1305) for the seal; builds the HPKE `info` (`"tls ech"‖0x00‖ECHConfig`), §6.1.3 padding, the two-step `beginSeal`/`Sealer` (exposes `enc` before the AAD to break the ClientHelloOuterAAD circularity), the outer `encrypted_client_hello` body, and the §7.2 acceptance confirmation (`HKDF-Expand-Label(HKDF-Extract(0, inner.random), "ech accept confirmation", transcript_hash, 8)`, HRR-label variant too). **`tls_client.zig`:** `Options.ech_config_list: ?[]const u8` (owned copy, freed once); when a usable config is present `start()` builds a ClientHelloInner (real SNI, real session_id, inner marker) sealed into a ClientHelloOuter (SNI = `public_name`, AAD = the outer with the payload zeroed), retains the inner hello + a distinct inner random; on ServerHello it recomputes the accept signal over `inner‖ServerHelloECHConf` and, on a match, switches the running transcript to the inner (else keeps the outer + authenticates `public_name`); `echOffered()`/`echAccepted()` accessors + an ECH-aware cert-verify name. **Default off / no usable config ⇒ byte-identical** (`writeClientHello` untouched; ECH stands down for resumption/0-RTT and refuses ECH+HRR). Tests: ECHConfigList parse (valid + 5 malformed forms + mandatory-ext skip + public_name validation); seal round-trips through a config holder that opens the payload and recovers the real inner SNI; the outer carries `public_name`; the accept signal switches to the inner transcript and a flipped bit rejects; byte-identical-when-off (null + unsupported-KEM ⇒ no `0xfe0d` ext, real SNI); accept-confirmation determinism/sensitivity + SHA-384 length checks. **Deferred (explicit):** DNS HTTPS-RR fetch, server-side ECH, `ech_outer_extensions` compression, ECH-over-PSK/0-RTT, ECH+HRR, and confirming the AAD convention + broader HPKE suites against a reference ECH server. **zig-review: Approve-with-nits** (memory-safety/free-once, byte-identical-when-off, the confirmation crypto, the two-step-seal AAD-circularity break, and the inner/encoded session_id split all CONFIRMED correct). Fixes applied post-review: (a) **AAD is now the header-LESS ClientHelloOuter body** with the ECH payload zeroed — matching the header-less EncodedClientHelloInner plaintext and the reviewer's §5.2 reading (single flip-point in `buildOuterEchBody`, still to be pinned against a reference vector before live ECH); (b) `prepareEch` is now truly **fail-safe** — a malformed list or a KEM-matching-but-wrong-pubkey-length config stands down to a byte-identical plain ClientHello instead of aborting `start()` (+`public_key.len==32` sealability guard, idempotency guard); (c) LOW nits: `effectiveVerifyName` defensive `orelse`, `isValidPublicName` rejects all-numeric TLDs, free-prior-`ech_inner_hello` on re-entry, `proto/ech.zig` cross-ref note. Full suite + `zig fmt` green. **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE cont. — 2.3 encrypted post-ServerHello alerts + TLS 1.2 alert leg landed.** Extends the plaintext-alert work: `Server.takeAlert` is now state-dispatched — pre-ServerHello → plaintext (unchanged); post-ServerHello (`wait_client_cert`/`_verify`/`_finished`) → ENCRYPTED via new `alertRecordEncrypted` under `server_app_keys`/`app_write_seq` (the server switches to app keys right after its Finished — 0.5-RTT — and nothing consumes app_write_seq before those states, so it's 0). TLS 1.2 gets a `takeAlert` too (plaintext for full-handshake wait states, encrypted under `keys.server_write`/`hs_write_seq` for resumed). Seq bumped only after a successful seal + `driveTls` closes right after ⇒ no reuse/nonce-reuse. `connected`-state faults stay bare-close (scoped). Genuine-handshake round-trip-decrypt tests (mutation-verified). Full suite green; zig-review **Approve-with-nits** (key/seq correct per state, no nonce reuse, switch exhaustive, no import cycle — sole nit a stale server.zig comment, fixed). **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE cont. — 1.3 RSA-PSS-256/384/512 cert-signature verification landed (SECURITY-CRITICAL).** The X.509 `rsassaPss` OID carries hash/MGF/saltLength in the AlgorithmIdentifier *parameters*, so a raw `sig_alg_params` channel is now threaded through `LinkInfo`/`CertParts` → `x509_verify.verifyCertSignature` (new arg) + the `tls_client` cert-chain path; a fail-closed `parsePssParams` (hash ∈ {256,384,512} — SHA-1 default REJECTED; MGF1 must be over the SAME hash; trailerField 1; saltLength ≤ 512) drives `rsa_verify.verifyPss` (+ an added ReleaseFast overflow guard on `em_len < h_len+salt_len+2`). Both `verifyCertSignature` callers updated (`verifySignedBy` + the delegated-OCSP `delegateAuthorizesResponse`); all PKCS1/ECDSA/Ed25519 paths byte-identical. Test signatures via a self-verifying M1279 Mersenne modulus (no keygen) + a real OpenSSL 2048-bit PSS vector. **NOTE: first attempt failed silently on the stale `50ca052` worktree base (rationalized the mismatch instead of resetting) — redone with a hard base-tripwire; all subsequent agents carry it.** Full suite green (6123); zig-review **Approve** — forgery-focused, all 10 fail-closed invariants verified (absent-hash rejection, MGF-hash binding, saltLength overflow-safety, OID-strict scheme selection, EMSA-PSS-VERIFY §9.1.2 correct). Low follow-ups: DRY the two PSS bodies; TLS 1.2 client PSS parity. **not deployed.**
- 2026-07-06: **4.2 CRL revocation WIRED into the TLS 1.3 client — Phase 4 CRL "wire" complete.** New `Options.crl: ?[]const u8` (caller-supplied CRL DER, e.g. from a CDP fetch) is duped into an owned `Client.crl` (errdefer-ordered after anchors/alpn/name, freed once in deinit). `checkCrlRevocation(crl_der, issuer_spki, leaf_serial, now)` runs after chain-verify + the OCSP-staple block in `parseAndVerifyCertificate`, FAIL-OPEN by design: `now==null` / unparseable / signature-not-authentic-under-leaf-issuer / stale each return void (ignore the CRL); ONLY an authentic + current CRL that lists the leaf serial returns `error.CertificateRevoked` (a CRL can authenticate a *revocation*, never clear a bad chain — forgery already hard-failed upstream; OCSP-staple stays hard-fail). Default null ⇒ block skipped ⇒ byte-identical for every existing caller. Three hermetic tests over a self-contained Ed25519 signed-CRL-with-revoked-serial fixture cover all six branches (positive revoke, absent-serial pass, wrong-issuer fail-open, stale fail-open, no-clock, unparseable) — non-vacuous (`expectError` on the revoke, bare-`try`=clean-void on the fail-opens). zig-review **Approve** (fail-open composition strictly gated auth→currency→lookup; free-exactly-once on every init/deinit path; no dangling borrow; the one revocation-suppression branch `isSerialRevoked`'s `catch return false` proven unreachable on any parse-accepted CRL — parse's `validateRevokedCertificates` walks the same slice deeper). Full suite green (6160/6164, 4 skipped, 0 failed). **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE cont. — 4.1 SCT + 4.2 CRL verification machinery landed (Phase 4 "wire", not "cut").** **4.2 CRL:** `crl.verifyCrlSignature(crl_der, issuer_spki)` — the revocation gate: parse → enforce inner==outer sig-alg OID (RFC 5280 §5.1.1.2) → verify the CRL's outer signature against the CALLER's issuer key via `x509_verify.verifyCertSignature` (never the CRL's own key) → `isSerialRevoked` (canonical-DER byte compare) + `crlIsCurrent`; new `x509.parseCrlDistributionPoint`. zig-review **Approve** (no revocation-bypass; wrong-issuer-key rejection non-vacuous). **4.1 SCT:** `sct.verifySctAgainstLogs` — RFC 6962 signed-data reconstruction + pinned-log (`CtLog`) matching + RSA/ECDSA log-key verify, fail-closed; new `x509.parseSctList`/`findSctListExtension`. zig-review **Approve-with-nits** (fixed the `16→14` header-constant that made the overflow test vacuous). **Integration:** both agents built on base 8488559 (pre-1.3) so the CRL call was threaded to 1.3's now-5-arg `verifyCertSignature` (null params — CRLs use PKCS1/ECDSA/Ed25519); the two independent `x509.zig` additions 3-way-merged (no name collision). **Wiring:** 4.2 CRL revocation is wired into `tls_client` (next commit, config-gated caller-supplied CRL, fail-open); 4.1 SCT enforcement needs embedded-SCT precert-TBS reconstruction (tracked follow-up — marginal for the ACME client). GOTCHA: `zig build check` (main.zig root) lazily skips unreferenced fns, so an undefined-OID / stale-arity slipped past it — only the full `zig build test` (runs the tests) caught them. Full suite green. **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE cont. — kTLS RX cmsg-demux landed (3.1 follow-up).** A kernel-decrypted TLS control record no longer drops the conn. On the recv-error branch for a kTLS-RX-offloaded conn, a synchronous `recvmsg` + `TLS_GET_RECORD_TYPE` cmsg (SOL_TLS) recovers the record's content_type out-of-band (io_uring `recv` stays the app-data fast path). `ktls.recvmsgRecordType`/`parseRecordTypeCmsg` (bounds-checked cmsg walk, `MSG_CTRUNC`-honored, exact LP64 geometry); `tls_conn.classifyKtlsRecord`; `server.handleKtlsRxControl` (bounded drain=16): app_data→feed, KeyUpdate→consume+continue (no drop), close_notify/EOF→graceful close, EKEYEXPIRED→"rekey unsupported" close. On-kernel test PROVES the demux (client KeyUpdate → record_type=22, never delivered as app-data). Default off ⇒ byte-identical. **Caveat:** a re-keying client still ends in a CLEAN close (kernel RX key not re-installed — RX-rekey is a tracked follow-up); close_notify + non-rekeying clients fully handled. Fixed 2 review nits (clear awaiting_pong on app-data drain; accurate EOF reason). Full suite green (incl. on-kernel test); zig-review **Approve** (cmsg memory-safe, recv-path re-arm/close correct, no plaintext-misdelivery window, cross-platform clean). Live-daemon KeyUpdate smoke test flagged pre-deploy. **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE cont. — 0.2 coverage-guided fuzz targets landed.** Added `std.testing.fuzz` targets (one per attacker-facing wire parser: x509, tls_record, ocsp, tls12 handshake, cert_compression bomb-guard, sni) behind a new `zig build fuzz` step (`zig build fuzz --fuzz` drives Zig 0.16's builtin coverage-guided fuzzer). Under a plain `zig build test` each target replays only its seed corpus once (bounded) + one 4000-iter deterministic bit-flip test, so the ~6100-test suite is NOT perturbed (mirrors the `ct-check` gating). No crashes found. Full suite green (6137). **not deployed.**
- 2026-07-06: **4.1 SCT / Certificate Transparency WIRED into the TLS 1.3 client — Phase 4 SCT "wire" complete (opt-in, fail-open).** The 4.1 machinery (`sct.verifySctAgainstLogs` + `x509.parseSctList`/`findSctListExtension`) was unwired; the missing piece was **embedded-SCT precertificate-TBS reconstruction**. New `x509.buildPrecertTbs(out, leaf_der)` rebuilds the exact bytes an embedded SCT signs over: the leaf's TBSCertificate with ONLY the SCT-list extension (`1.3.6.1.4.1.11129.2.4.2`) removed and the three enclosing lengths (Extensions SEQUENCE, `[3]` EXPLICIT, outer TBS SEQUENCE) re-encoded to canonical minimal DER — every other extension preserved verbatim in order; a two-pass size-then-write with a single bounds check, fail-closed on malformed DER, `error.MissingField` if the leaf carries no SCT (handles the CA-signs-precert-directly case; Precertificate Signing Certificates out of scope → their SCTs simply fail to verify, which fail-open tolerates). Sized so a legitimate cert always fits the framing (`sct.max_precert_tbs_len`). **Wiring (mirrors 4.2 CRL):** `Options.ct_logs: []const sct.CtLog` (caller-supplied pinned logs) is DEEP-copied into an owned `Client.ct_logs` (slice + each `key_spki_der`, leak-tight `made`-counter errdefer, freed once in deinit) with `Options.enforce_sct`; `verifyEmbeddedScts(chain, ct_logs, enforce)` runs after chain-verify + OCSP + CRL in `parseAndVerifyCertificate`. OPT-IN + FAIL-OPEN: `ct_logs.len==0` ⇒ the whole path is skipped ⇒ wire byte-identical (verified); absent SCTs, an unpinned log, or any parse/reconstruction failure return void; ONLY `enforce_sct` + a pinned-log-matched, signature-INVALID SCT returns `error.BadSct`. Temporal (future-dated) check deliberately passed `null` so `.invalid` is scoped to genuine auth failures (bad sig / malformed / alg mismatch), never clock skew. **Tests:** x509 exact-byte reconstruction (SCT first/middle/last preserving order; **long-form DER length boundary**; no-SCT→MissingField; OutputTooSmall + malformed fail-closed); a hermetic e2e (real Ed25519 self-signed issuer for `extractCertParts`, synthetic leaf with a genuine ECDSA-log-signed embedded SCT over the reconstructed precert) proving valid→pass, tampered→`BadSct` ONLY under enforcement, and absent/unpinned/feature-off→pass; plus a deep-copy/free (no-leak, no-dangle) test that frees the caller SPKI before use. Full suite green. zig-review pending. **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE cont. — assurance batch: 0.4 + 0.1 landed.** (0.4) constant-time verification — a NEW opt-in `zig build ct-check` step (`tools/constant_time_check.zig`, NOT in `zig build test` so it can't flake the suite) runs a dudect-style Welch's-t timing check on ECDSA-P256 sign / X25519 scalarmult / RSA-2048 sign; all |t| < 2.4 (gate 10), proven non-flaky. RSA varies input (no keygen exposed → input-independence, the property blinding provides); ECDSA/X25519 vary the actual secret. (0.1) positive independent-vector KATs — X25519 iterative (RFC 7748 §5.2, 1 + 1000 iters) + ECDSA-P256/SHA-256 **verify** KAT (RFC 6979 §A.2.5), hex cross-checked against the RFCs. Honest scope: ECDSA **sign**-side skipped (our nonce is std deterministic-null, not RFC-6979 HMAC_DRBG → a sign-KAT would falsely fail); RSA-PSS skipped (no trustworthy vector sourced — "skip rather than invent"). Every embedded vector reproduced. Full suite green (0.1 KATs run in-suite); ct-check PASS. **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE cont. — 1.5 landed (RFC 8449 record_size_limit on the TLS 1.2 leg).** Mirrors the shipped 1.3 impl on `tls12_server`/`tls12_client`: parse+validate the peer's `record_size_limit` ([64, 2^14+1] else BadHandshake), offer (client) / solicited-only echo (server, both full + resumed flights), and fragment outbound app records in `encrypt()` to honor the peer's limit. New `tls_record.recordContentLimit12` (no `-1` — 1.2 has no inner content-type byte; hardened with `@max(1,…)`). Per-fragment nonce/seq is unique (seq++ per fragment → distinct GCM nonce + seq-bound AAD — reviewer verified NO nonce-reuse). Empty ⇒ byte-identical. 3-way merged the `tls12_client.zig` tail against 1.1 (kept both tests). Full suite green (6110); zig-review **Approve** (per-fragment nonce/seq correct, no-`-1` bound right, symmetric validation, resumption-safe, memory-clean). **not deployed.**
- 2026-07-06: **1.3 follow-up (#46 residual RESOLVED) — production TLS 1.3 client chain verifier ALIGNED to the shared `x509_verify` sig-alg set.** Mirrors #46 for `tls_client.zig`: `verifyChainToTrustAnchors` (trust-anchor loop) + `verifyIssuedBy` now build child+issuer via `x509_verify.linkInfo` and delegate the signature primitive to `x509_verify.verifySignedBy`, replacing a **duplicated** local `verifyCertSignature`/`verifyCertPss` dispatch (removed, along with the six now-orphaned OID consts — `oid_sha256_rsa`/`sha384_rsa`/`sha512_rsa`/`rsassa_pss`/`ecdsa_sha256`/`ecdsa_sha384`; no dead code). `extractCertParts`/`CertParts` are **KEPT** (still used by the DC/SCT/OCSP/CRL paths + the path-length self-issued check); the RPK/DC/SCT/ECH composition, leaf-key handling (`parsePublicKeyFromSpki`/`verifySignatureScheme`/ServerKeyExchange/CertificateVerify), and validity/EKU/basicConstraints/name-constraints/path-length logic are untouched. **Subtlety vs #46:** the 1.3 client's local `verifyCertSignature` had already been broadened and fully verified **P-384 (ecdsa-with-SHA384)** chain links end-to-end (real `.ecdsa_p384` issuer key), but the shared `x509_verify.verifyCertSignature` did NOT — so a naive delegation would have REGRESSED P-384 to `UnsupportedSigAlg`. To keep delegation a **strict superset with no regression**, `x509_verify` gained an **ecdsa-with-SHA384 / P-384** branch (`verifyEcdsaP384` + fail-closed `ecP384PointFromSpki`, which parses the P-384 issuer point straight from the SPKI **before** `extractPublicKey` — the `x509.SubjectPublicKey` union deliberately does NOT model P-384, avoiding the documented `oauth_jwt` exhaustive-switch blast radius). This additively broadens `tls12_client` + `verifySimpleChain` + CRL/OCSP signature checks to accept P-384 too (fail-closed; unforgeable without the P-384 key). `x509_selfsign` gained a `buildSelfSignedEcdsaP384` minter. Tests: 8 tls_client end-to-end cases (RSA-256/384, RSA-PSS, ECDSA P-256, **ECDSA P-384**, Ed25519 self-anchor; flipped RSA-PSS sig byte → `UnknownCa`; flipped P-384 sig byte → `UnknownCa`) + an `x509_selfsign` P-384 round-trip/tamper self-test through the shared verifier + a negative test that ecdsa-with-SHA384 over a non-P-384 (P-256) issuer key fails closed (`BadSignature`). Full unfiltered suite green (**6240 pass / 4 skip / 0 fail**); zig-review **Approve-with-nits** (strict-superset/no-bypass CONFIRMED — identical algorithm coverage for the 1.3 chain path, P-384 restores parity via the same `EcdsaP384` primitive the old leaf path used; fail-closed P-384 parse; RPK/DC/SCT/ECH + leaf-key + chain logic untouched; `extractCertParts` + all kept consts still wired; no UAF; error-set coercion sound; sole nit = doc-completeness, since folded in: CRL/OCSP added to the blast-radius note). **not deployed.**
- 2026-07-06: **#46 — TLS 1.2 client chain verifier ALIGNED to the full `x509_verify` sig-alg set (1.3 follow-up, PSS-for-1.2-client piece DONE).** The `tls12_client` cert-chain path previously handled only `oid_ecdsa_sha256` + `oid_sha256_rsa`, so a real CA link signed with **sha384WithRSA** or **RSASSA-PSS** (both common on public CAs) failed to anchor. `verifyChainToTrustAnchors`'s trust-anchor loop + `verifyIssuedBy` now build both child and issuer via `x509_verify.linkInfo` and delegate the signature primitive to the newly-`pub` `x509_verify.verifySignedBy` (which threads `sig_alg_params` into `x509_verify.verifyCertSignature`) — gaining **RSASSA-PSS + PKCS1 SHA-384/512 + ECDSA P-256 + Ed25519** in one move. Everything else in the verifier is byte-for-byte preserved: leaf SAN match, `validateParsedAt`, EKU serverAuth, `basic_constraints_ca`, `key_usage_cert_sign`, path-length, issuer/subject DN matching, and the `catch continue` per-anchor semantics. The narrow local `verifyCertSignature`/`CertParts`/`extractCertParts`/`algorithmOid` + the two dead OID consts were removed (no dead code); the ServerKeyExchange leaf-key path (`parsePublicKeyFromSpki`/`verifySignature`) is untouched — it just sources the leaf SPKI via `linkInfo` now. `x509_selfsign` gained a `sig_pss` minting flag (id-RSASSA-PSS / SHA-256 / MGF1-SHA-256 / salt 32) for a genuine end-to-end PSS test. Tests: 6 tls12 end-to-end cases (self-signed cert as its own anchor per algorithm — RSA-SHA256/384, RSA-PSS, ECDSA-P256, Ed25519 all anchor; a flipped signature byte → `UnknownCa`, isolating the sig check) + 1 x509_selfsign PSS round-trip/tamper self-test. Full unfiltered suite green (6168 pass / 4 skip / 0 fail); zig-review **Approve** (no-bypass delegation verified as a strict superset — no algorithm previously accepted is now rejected; byte-for-byte TBS-walk equivalence between `extractLinkInfo` and the removed `extractCertParts`; leaf-key path no UAF; PSS minting round-trips the shared verifier's real PSS branch; negative test isolates the sig check; all kept helpers still wired; enum-switch exhaustiveness held). **Residual follow-up:** the production 1.3 `tls_client.zig` still carries its own narrow local `verifyCertSignature` — apply the identical alignment there. **not deployed.**
- 2026-07-06: **MULTI-AGENT WAVE (worktree-isolated implementers, per-item full-suite + zig-review gate) — batch 1a landed 1.1 + 2.4.** (1.1) TLS 1.2 client now carries the RFC 8446 §4.1.3 downgrade-sentinel machinery (`checkDowngradeSentinel` + both sentinel constants + `error.DowngradeDetected`), wired into `parseServerHello`. **Inert by construction** — this 1.2-only client offers only 1.2, so per §4.1.3 the sentinel is not a downgrade signal for it, AND our own 1.2 server stamps `DOWNGRD\x01` unconditionally so a 1.2-only client that aborted couldn't self-handshake; flipping one `client_offered_tls13` flag activates the conformant abort if 1.3 is ever offered. Self-reviewed (additive+inert). (2.4) daemon multi-cert SNI plumbing — repeatable `[[tls.sni]]` config (matches the `[[opers]]` array-of-tables idiom), each entry loaded via the same `tls_certs` loader, retained for the server lifetime, projected to `tls_server.Config.sni_certs`; `server.Config.tls_sni_certs` carrier + `tls13Config` wiring. Empty ⇒ byte-identical. zig-review **Approve** (all 6 ownership/hygiene invariants: `built` freed once, borrowed slices survive ArrayList growth, keys secure-zeroed, LIFO teardown, fail-fast-before-wiring, symmetric parse/free). Medium follow-up tracked: extract a `buildSniCerts` helper + leak tests for the main.zig load loop. **not deployed.** (Worktree gotcha: agent worktrees started ~725 commits stale at 50ca052 — agents caught it + reset to HEAD.)
- 2026-07-06: **2.3 TLS alert mapping (server-side, pre-ServerHello).** A handshake rejection now sends a fatal TLS alert (RFC 8446 §6) before closing instead of a bare TCP reset, so the peer learns WHY. `tls_server` maps errors → §6 codes (`alertDescriptionForError`: ProtocolVersion→protocol_version, MissingExtension→missing_extension, unsupported-suite/group→handshake_failure, BadHandshake→decode_error, cert/key config→internal_error, else→handshake_failure; OutOfMemory→no alert) and encodes a plaintext alert record; `Server.takeAlert` gates on `state ∈ {wait_client_hello, wait_second_client_hello}` (before any ServerHello ⇒ no keys ⇒ plaintext correct). `TlsConn.takeAlert` delegates (undecided⇒plaintext, tls12⇒null). `driveTls` queues the alert on the `onInbound` error path; it rides the existing drain-then-close (armSendIfNeeded flushes it, then the conn closes). Scoped to pre-ServerHello (the majority of rejections); **encrypted post-ServerHello alerts (FinishedMismatch, bad client cert) + the 1.2 leg are a documented follow-up** (bare close there, as before). Unit tests (mapping + record well-formedness + state gate). Full suite green. **not deployed.**
- 2026-07-06: **2.4 follow-up: boot-time leaf↔signing-key type check.** `tls_server.Server.init` now fails fast with `LeafKeyMismatch` when a configured signing key's type (rsa/ecdsa-p256/ed25519) can't match its leaf's subjectPublicKey — for the default cert AND every `sni_certs` entry — instead of silently producing a CertificateVerify every client rejects. Conservative: an unparseable/unrecognized leaf is allowed (never false-rejects a valid cert). `leafKeyKindMatches` compares `x509.extractPublicKey` tag vs the resolved `SigningKey`. Negative test (Ed25519 leaf + ECDSA key ⇒ reject) + the matching pairing still inits; existing handshake tests are the positive guard. Full suite green. **not deployed.**
- 2026-07-06: **2.1 follow-up: routed the two remaining direct-only OCSP callers through the chain-aware verifier.** `ocsp_staple.zig`'s CRITICAL revoked-own-leaf log and `tls_client.zig`'s inbound-staple check (`verifyOcspStapleForLeaf`, now takes the client's `?i64` clock) both call `verifyResponseSignatureWithChain` — so a delegated-responder-signed revocation is no longer missed and a legit delegated staple is no longer rejected as `BadCertificate`. No clock ⇒ direct-only fallback (fail-closed). Full suite green; underlying verifier already zig-review Approve. **not deployed.**
- 2026-07-06: **2.1 follow-up: delegated OCSP responder support COMPLETE.** `isStapleServable`/OCSP-stapling verification previously accepted only directly-issuer-signed responses, so it rejected the many real-world CAs that serve OCSP via an issuer-signed `id-kp-OCSPSigning` responder cert. Added `verifyResponseSignatureWithChain`: tries direct signing first, else authorizes an embedded `certs [0]` responder cert — must (a) carry the exact `id-kp-OCSPSigning` EKU (anyEKU does NOT count), (b) match the response's ResponderID (byName or byKey SHA-1), (c) be valid at `now`, (d) be signed by the trusted issuer's key (`x509_verify.verifyCertSignature` against `issuer_spki_der` — the crypto gate that stops a forged self-signed responder), then (e) the response verifies under the responder's key. Plumbing: `x509` gained an `eku_ocsp_signing` flag (exact-OID); `x509_selfsign` gained an issuer-signed-cert + EKU test builder; `ocsp` captures the ResponderID + `certs [0]`. Tests: byKey-primitive/byKey-EXPLICIT/byName accepts, plus rejects for wrong-issuer, missing-EKU, ResponderID-mismatch, and wrong-signer (isolating check (e)); malformed `certs` sibling is skipped not fatal. Full suite green; zig-review **Approve** (no-bypass argument verified — (d) checks the embedded cert against the caller's issuer key, never the cert's own; exact-OID EKU enforced; fail-closed). **not deployed.**
- 2026-07-06: **1.6 ticket-key rotation COMPLETE — the USR2 carry landed.** The rotation trigger (REHASH `rotateTicketKey`: current→previous, mint fresh) + `openTicketWithRotation` were already in place; the missing piece was that a USR2 upgrade minted a brand-new random ticket key in the successor, so every resumption ticket died on upgrade. Fixed with a singleton `helix/ticket_key_capsule.zig` (`{current, previous?}`) rid on the sealed handoff arena: `CapsuleKind.tls_ticket_keys`; `performUpgrade` emits one piece (gated on `tls_enable_resumption`, owned by `blobs`, mirroring `.ws_session`); `adoptInheritedSessions` "Pass 0" overrides the successor's boot key before any handshake (incl. re-attached conns → a post-upgrade NewSessionTicket seals with the carried key). Missing/undecodable ⇒ boot key kept (graceful); resumption-off ⇒ byte-identical. Capsule round-trip + truncation/bad-version tests; full suite green; zig-review **Approve** (blob ownership no double-free/UAF, Pass-0 override lands in time, registry entry load-bearing, decode OOB-safe, secret hygiene matches `tls_snapshot`). **not deployed.**
- 2026-07-06: **kTLS RX live wiring committed+pushed (26624cf); 2.4 CH2-SNI hardening COMPLETE.** kTLS RX (entry below) landed on the github remote after zig-review **Approve** — **kTLS TX+RX both done** (gated `[tls] ktls=tx`/`txrx`), NOT deployed (needs a kTLS-capable deploy-kernel + live-interop verify). **2.4 SNI follow-up:** a client that changes its SNI across a HelloRetryRequest is now rejected (RFC 8446 §4.1.2 forbids changing it). `Server.ch1_sni_digest` (SHA-256 of ClientHello1's host_name) pins the SNI; ClientHello2 must present a byte-identical host_name (digest compare) or the handshake fails `BadHandshake`, and `sni_cert` is pinned on CH1 (no re-selection on CH2) — closing a mid-handshake cert-selection swap. `selectSniCert` refactored to a single extraction site; pure `sniDigest`/`sniDigestEql` helpers. Tamper-CH2 integration test + helper unit test; the existing HRR loopback test is the happy-path guard. Full suite green (6092); zig-review **Approve** (state-machine gate sound, exhaustive optional-digest semantics, no dangling borrow, tamper test fires before Finished). **not deployed.**
- 2026-07-05: **2.1 server OCSP stapling COMPLETE** (inc 6-8): servable gate + generalizedTimeToEpoch + http_fetch.post (inc 6); server publish/swap plumbing (inc 7); `ocsp_staple.Service` fetcher + `[ocsp]` config + main wiring (inc 8). Full suite green; **not deployed** (held for deploy gate). Remaining follow-ups are low-priority (1.2 client round-trip test, ~~delegated responders~~ **DONE**, ~~fetch backoff~~ **DONE**).
- 2026-07-05: **2.1 follow-up: OCSP fetch exponential backoff landed.** The `ocsp_staple.Service` retried a down responder every 15-min check interval; now `fetchAndPublish` returns success/failure and consecutive failures back off exponentially (`backoffSeconds`: min_refresh doubling per failure, clamped to max_refresh) via a `next_retry_unix` gate, reset on a successful publish or a leaf-serial change (a rotated cert gets a fresh attempt). Pure `backoffSeconds` unit-tested. Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS RX OFFLOAD COMPLETE (live recv-path + USR2).** After experimentally confirming a control record (client KeyUpdate) makes plain `recv()` return an error (never app-data plaintext) — which validates treating control records as a conn drop — the live wiring landed: `ConnState.tls_rx_offloaded` + `Config.tls_ktls_rx`; `driveTls` attaches RX one-shot at the connected transition when `hasBufferedInbound` is false (else stays userspace RX); `driveClientBytes` routes an RX-offloaded conn's kernel-decrypted plaintext to `feedBytes`/`driveWs`; USR2 `rx_offloaded` snapshot marker + adopt (path-A). main.zig `ktls=txrx` gate + boot log. Default off ⇒ byte-identical. **kTLS TX+RX both complete now.** Follow-up: control records currently drop the conn (no `recvmsg`+cmsg demux yet — a client KeyUpdate reconnects); needs live-daemon interop verification before deploy. Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS RX attach machinery + kernel validation landed** (the safe half of RX offload, mirroring the TX foundation). `Server.ktlsRxParams()` (client_app_keys + app_read_seq, the decrypt direction), `ktls.attachRx` (setsockopt TLS_RX), `TlsConn.buildKtlsRxCryptoInfo`/`enableKtlsRx`, and `attachUlp` now tolerates EEXIST (so a txrx conn shares one ULP attach across TX+RX). **Integration test PROVES it: the client encrypts a record, the KERNEL decrypts it, and `recv()` returns the plaintext.** Still DEFERRED (the risky recv-hot-path half): switch the io_uring recv to `recvmsg` + `TLS_GET_RECORD_TYPE` cmsg to demux control records, retire the userspace `pending_recv` partial-record buffer, handle client KeyUpdate arriving as a control record (re-`setsockopt(TLS_RX)`), and the USR2 RX-seq caveat (drop offloaded conns on upgrade, or reconcile). Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS TX OFFLOAD COMPLETE (live send-seam + USR2).** The atomic wiring landed: `ConnState.tls_tx_offloaded`/`ktls_attach_pending`, the `driveTls` pending-mark + drain-gated `maybeAttachKtlsTx` (attaches at connected-transition / send-completion once inline+overflow send bufs drain), the `appendSecuredToConn` plaintext branch, post-handshake-record drop (no KeyUpdate/NST double-encrypt), `[tls]` daemon-config gate (`tls_ktls_tx`), main.zig activation + boot log, and the USR2 `tx_offloaded` snapshot marker + adopt (kernel TX state survives execve on the carried fd — no re-attach; `pending_out` carried as plaintext). **Live integration test PROVES it: the kernel encrypts a server write with our crypto_info and `tls_client` decrypts it** — plus snapshot flag round-trip + tolerant-decode tests. Default off ⇒ byte-identical; full suite green incl. the USR2 zero-drop tests; zig-review **Approve** (all 7 focus areas SOLID — hot-path no-op-when-off, attach-timing self-synchronizing via live `app_write_seq`, USR2 seq continuous, attach-failure safe). **Operator note:** an offloaded TLS 1.3 client that sends `KeyUpdate(update_requested)` is dropped and reconnects (rare; documented Phase 1 limitation — the kernel owns TX keys). **not deployed** (deploy-kernel `ircx.us` must be kTLS-capable — the boot probe now reports this). RX offload (`txrx`) still deferred.
- 2026-07-05: **3.1 kTLS `TlsConn` attach bridge landed.** `TlsConn.buildKtlsTxCryptoInfo` (pure: `ktlsTxParams` → cipher map → `tls13CryptoInfo` → encode) + `TlsConn.enableKtlsTx(fd)` (build + `attachUlp` + `attachTx`) — the per-connection adapter's attach action, TLS-1.3-only (1.2 offload deferred). Loopback test drives a real `TlsConn` handshake and asserts `buildKtlsTxCryptoInfo` yields a 40-byte AES-128-GCM crypto_info (version 0x0304, cipher_type 51) from live keys — the same shape the kernel accepted in ktls.zig's TLS_TX test. **Remaining live wiring:** the io_uring drain-timing trigger that calls `enableKtlsTx` at the connected transition once `send_buf` drains, the `ConnState.tls_tx_offloaded` field + `appendSecuredToConn` plaintext branch, KeyUpdate/NST gating, and the USR2 marker — the delicate hot-path/crown-jewel integration. Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS engine→kernel bridge landed — the Phase 1 toolkit is now COMPLETE.** `tls_server.Server.ktlsTxParams()` returns the connected session's TX offload material — the negotiated cipher (neutral `KtlsCipher`, decoupled from the daemon), the app write key, the 12-byte static write IV, and `app_write_seq` — or null before the handshake. Loopback test asserts it's null pre-handshake and matches the live `server_app_keys` + suite (AES-128-GCM) post-handshake. **Everything Phase 1 needs now exists + is tested/kernel-validated:** config mode · capability probe · crypto_info encoders (kernel-validated) · attach primitives (kernel-validated) · this engine→kernel bridge. **The ONLY remaining piece is the live send-seam integration** (daemon: attach at the connected transition after send_buf drains → `appendSecuredToConn` plaintext branch → gate off post-handshake KeyUpdate/NST → the USR2 offloaded-marker) — the coupled hot-path + crown-jewel-upgrade work that warrants a focused, deploy-kernel-verified session. Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS `[tls] ktls` config landed.** `Config.KtlsMode` (off/tx/txrx, default off) parsed from `[tls] ktls`, projected through `TlsBootConfig`, and consumed by the `main.zig` boot report — which now crosses the operator's configured intent with the kernel's TLS-ULP capability (e.g. "kTLS tx requested; kernel is kTLS-capable" vs "requested but no TLS ULP"). Unknown mode → hard `ParseError`. Config parse tests + default assertion. No offload yet (mode only widens the boot report until the Phase 1 send-seam lands). Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS attach primitives + real-kernel encoder validation landed.** `ktls.attachUlp` (setsockopt SOL_TCP/TCP_ULP="tls") + `ktls.attachTx` (SOL_TLS/TLS_TX from an encoded `crypto_info`) — the exact setsockopt calls Phase 1's send-seam will make. A gated loopback test establishes an ESTABLISHED TCP pair and installs our encoded TLS 1.3 `crypto_info` via TLS_TX for AES-GCM-128 (hard-asserted; universal on any kTLS kernel) + AES-GCM-256/ChaCha20 (best-effort per kernel support) — **the kernel accepting the struct validates our byte layout (header/field-order/sizes/rec_seq) against a real kernel**, closing the "self-authored vectors" gap. Passed on the dev kernel (7.0.3). Still no live send-seam wiring (that + USR2-marker + config is Phase 1). Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS Phase 0 capability probe landed** — `ktls.ulpAvailable` (pure token parser, unit-tested) + `ktls.probeUlpSupport()` (reads `/proc/sys/net/ipv4/tcp_available_ulp`), wired into `main.zig` so a `[tls]`-enabled boot logs whether the kernel offers the TLS ULP — the "is the deploy kernel kTLS-capable?" check the design flags, now answered from the logs (informational; TLS still userspace). Per-suite `TLS_TX` acceptance probe + the `setsockopt` attach primitives remain Phase 1 (need an ESTABLISHED socket + deploy-kernel round-trip). Full suite green; **not deployed**.
- 2026-07-05: **3.1 kTLS Phase 0 foundation landed** — new pure `src/daemon/ktls.zig` (no syscalls, no live wiring): kernel-UAPI constants, per-cipher geometry, `CryptoInfo.encode` byte serializer, `seqToBytes`, and the TLS 1.3 salt/iv split, all golden-vector unit-tested (7 tests). Byte layout is authoritative (UAPI); TLS 1.2 explicit-nonce derivation + the live `setsockopt` attach/probe/self-test/config/USR2-marker are deferred to Phase 1 (need a deploy-kernel round-trip). Full suite green; **not deployed**.
- 2026-07-05: **Phase-C cross-completion UAF FIXED.** The daemon freed the superseded TLS generation *inline* on cert reload (`reloadTlsCertsLocked`) and OCSP-staple swap (`maybeSwapOcspStaple`), so a handshake that captured a cert-chain/staple slice at accept time could read freed bytes if a swap committed mid-handshake. Fix: retain the just-replaced generation one extra cycle (`reload_tls_prev`/`reload_tls12_prev`/`ocsp_staple_prev`) and free only the generation from two swaps ago — reloads are cadenced in hours/days, handshakes complete in ms, so no live handshake can reference a two-generations-old buffer. deinit frees the retained generations; the existing A→B→C reload test (testing allocator) now also asserts B is retained as `reload_tls_prev` and proves no leak/double-free. Full suite green; **not deployed**.
- 2026-07-05: **2.4 SNI-based cert selection COMPLETE (library) — Phase 2 done.** `Config.sni_certs` + per-ClientHello `selectSniCert` (reuses `proto/sni.zig` on `raw`) + `serverNameMatches` (exact/case-insensitive/`*.`-wildcard) + `activeCertChain`/`activeOcspStaple`/`activeSigningKeyResolved` accessors so `writeCertificate`/`writeCertificateVerify` present the SNI-matched cert. Empty `sni_certs` ⇒ byte-identical. Multi-cert loopback (A/B/default) + matcher unit test. `zig-code-reviewer`: **Approve** (cert/key never desync across default/HRR/PSK/mTLS — all key off the one `sni_cert` index set before any Certificate is written; SNI parse bounds-safe; wildcard boundaries correct). Follow-ups (LOW, deferred): (a) enforce CH2 SNI == CH1 SNI across HRR (harmless today — transcript-authenticated — but cheap strict-compliance); (b) `writeCertificateVerify` doesn't consult client `signature_algorithms` and `init` doesn't check each entry's key-type matches its leaf (pre-existing, applies to single-cert too); (c) daemon-side multi-cert config plumbing (no consumer yet; this is the ECH prereq). Full suite green; **not deployed**.
- 2026-07-05: **2.3 certificate compression (RFC 8879) COMPLETE — both directions.** New shared `proto/cert_compression.zig` (zlib via `std.compress.flate` `.zlib`, no new dep) with the 3-layer decompression-bomb guard. Server negotiates `compress_certificate(27)`, gated behind default-off `enable_cert_compression`, and emits `CompressedCertificate(25)` only when it shrinks (else plain). Client advertises zlib + decodes type-25 under the bomb guard. zlib-only (std has no brotli, zstd decode-only). Loopback round-trip + fallback + module bomb tests. Full suite green; **not deployed**. The design's phase-1-first (server-send-only) rationale was deploy-risk; since nothing is deployed the two phases shipped together for a clean round-trip test. The client offer is gated behind default-off `offer_cert_compression` (symmetric with the server), so the outbound ACME ClientHello stays byte-identical and no decode path is reachable until opted in; an unsolicited CompressedCertificate is rejected. `zig-code-reviewer`: **Approve** (bomb guard airtight — verified the `.limited(declared_len+1)` `+1` is load-bearing against std source; transcript folds identical bytes both ways; memory-clean; default-off gate byte-identical). Only LOW notes: 64 KiB cap asymmetry (deliberate), optional `bad_certificate`/`decompression_failure` alert mapping.
- 2026-07-05: **2.2 server HelloRetryRequest COMPLETE.** Server parses `supported_groups`, HRRs (once) for the preferred mutually-supported group when no usable key_share is offered, rewrites the transcript with the `message_hash` synthetic, and pins ClientHello2 to the requested group. Reused `supported_groups.selectPreferred`. Turned the in-repo `tls_client`'s previously-untested HRR handling into a real, RFC-compliant path (single-share CH2, offered-share-aware rejection) to drive a full loopback interop test. `zig-code-reviewer`: **Approve-with-nits, no CRITICAL/HIGH** (transcript/pinning/no-loop/0-RTT-ordering/memory-safety all verified); applied both MEDIUM CH2-hardening fixes — **M1** reject `early_data` in CH2 (§4.1.2/§4.2.10 MUST), **M2** reject a CH2 cipher-suite change vs the HRR (`hrr_suite`). Full suite green; **not deployed** (held for deploy gate). Follow-ups (low-pri): full CH1↔CH2 consistency diff (we only pin group+suite+early_data), HRR cookie emission (stateless retry), no-overlap negative test needs a hand-built/x448-capable client, and client-side L2 (tls_client re-emits `early_data` in CH2 on the resume+0-RTT+HRR combo — unreachable via ACME).
- 2026-07-06: **5.3 raw public keys (RFC 7250) — CLIENT SIDE landed.** New `server_certificate_type(20)`/`client_certificate_type(19)` codecs in `proto/tls_extension.zig` (a `CertificateType{x509=0, raw_public_key=2}` enum + fail-closed `buildCertTypeList`/`parseCertTypeList`/`parseSelectedCertType`). `tls_client` gained an opt-in `Options.offer_raw_public_key` (default false): when on, the ClientHello offers `server_certificate_type=[RawPublicKey,X509]` (placed before any pre_shared_key, which must stay last); `parseEncryptedExtensions` consumes the server's echoed selection (rejects an *unsolicited* response or an unoffered type, and `client_certificate_type` is always unsolicited since we present no client cert); and when the server selects RawPublicKey, `parseAndVerifyCertificate` branches to `parseRawPublicKey`, which reads the single CertificateEntry's bare `SubjectPublicKeyInfo` as the handshake-signature key via the existing `parsePublicKeyFromSpki` (NO new crypto). Trust model: a raw key has NO chain/CA — it does NOT bypass the X.509 trust checks, it is a distinct TOFU/pinning path reachable only when opted in AND negotiated (default `.x509` always runs the full chain verify). The RSA-borrows-`hs_plain` copy (`ownLeafRsaKey`, extracted + shared) now protects the RPK path too. Tests: extension-codec round-trips + fail-closed framing; ClientHello byte-identical-when-off (no type 19/20 on the wire) + offer-when-on; a constructed bare-SPKI (Ed25519) Certificate accepted with no anchors → leaf key set → a real server CertificateVerify signed by that raw key VERIFIES (tamper → BadSignature); the SAME bare SPKI REJECTED on the default X.509 path (no-bypass proof); EE rejects unsolicited/unoffered `server_certificate_type`. **Server-side offering + `client_certificate_type` deferred (follow-up).** Full suite green (6172/6176, 4 skipped, 0 failed, unfiltered); zig-code-reviewer: **Approve-with-nits** — no CRITICAL/HIGH/MEDIUM; the no-X.509-bypass argument and byte-identical-off gate both traced to source + tests; two optional LOW nits (the EE consume path is now *stricter* — an unsolicited recognized cert-type extension is rejected per §4.2, a behavior change but strictly-more-correct and inert vs conforming servers; optional golden-vector ClientHello). **not deployed.** (Worktree gotcha AGAIN: the isolation base was the stale `50ca052` — rebased onto HEAD `d67f01f` before implementing, per the wave-2 tripwire.)
- 2026-07-06: **4.1 SCT presence-enforcement — `require_sct` distinct-log quorum added (closes the 4.1-review MEDIUM).** The prior 4.1 wiring only had `enforce_sct` (tamper-detection): it rejects a *present, pinned, signature-INVALID* SCT but does nothing about a mis-issued cert that embeds **NO** SCT, or SCTs only from **unpinned** logs — both bypassed enforcement by yielding a fail-open early return. New opt-in `Options.require_sct: u8` (the CT policy threshold N): when `>= 1` AND `ct_logs` is non-empty, the leaf MUST present ≥ N embedded SCTs that each verify against a **DISTINCT pinned log** (RFC 6962 §5.1-style quorum — duplicate SCTs from one log count once), else `error.InsufficientScts`. `sct.ListSummary` gained `distinct_valid_logs` (counted in `verifyList` via a `max_scts`-bounded seen-set of valid SCT `log_id`s; an `.invalid`/`.no_applicable_log` SCT never contributes). `verifyEmbeddedScts` was restructured so every fail-open path (missing chain, no SCT extension, precert-TBS reconstruction failure, malformed SCT list) `break`s a **labeled block with a ZERO summary** and falls through to a single decision point — so under `require_sct` those "no valid SCT proven" outcomes become a hard reject, while `require_sct == 0` (default) skips the new gate entirely ⇒ behavior byte-for-byte identical to before (fail-open). Composes with `enforce_sct` (a signature-invalid pinned SCT still `BadSct`; an invalid SCT never counts toward the quorum). **OPT-IN / byte-identical-when-off proven:** empty `ct_logs` short-circuits before any policy (a `require_sct=2` no-op test on a broken leaf), and `require_sct=0` leaves absent-SCT leaves passing. Tests: `sct.verifyList` distinct-log counting (2-from-same-log ⇒ distinct=1; 1-from-each-of-2 ⇒ distinct=2; invalid/unpinned ⇒ 0; genuinely-distinct-key assert = non-vacuous) + 4 `tls_client` e2e cases over a multi-log fixture (`sctFixtureSignWithLog` mints fresh distinct logs signing the same precert; `sctFixtureLeafMulti` embeds many SCTs): distinct-log quorum pass/over-threshold reject, duplicate-log can't-satisfy-2, absent/unpinned mis-issuance gate (with the non-vacuous "pins ⇒ pass" check), and require_sct×enforce_sct composition. Full unfiltered suite green; zig-code-reviewer: **PENDING**. **not deployed.**
- 2026-07-06: **5.3 raw public keys (RFC 7250) — SERVER SIDE landed (Phase 5.3 client+server complete).** `tls_server` gained an opt-in `Config.enable_raw_public_key` (default false ⇒ EncryptedExtensions + Certificate byte-identical). When on AND a client offers `server_certificate_type` including RawPublicKey (parsed fail-closed via the existing `tls_extension.parseCertTypeList` into a `[maxInt(u8)]` buffer — overflow structurally impossible; a malformed list ⇒ `error.BadHandshake`), the server sets a per-ClientHello `server_cert_type_rpk` flag, echoes the single-value selection in EncryptedExtensions (gated `… and !self.resumed`, since resumption sends no Certificate), and `writeCertificate` early-returns to a new `writeRawPublicKeyCertificate` that emits one CertificateEntry whose `cert_data` is `x509.parse(activeCertChain()[0]).spki_der` (the full leaf `SubjectPublicKeyInfo`) — no chain, no per-cert extensions, no OCSP staple. CertificateVerify is UNCHANGED (still signed with the leaf key — Ed25519/ECDSA-P256/RSA-PSS all covered). The classic X.509 `writeCertificate` body is literally untouched (byte-identical gate: a plain server and a feature-on-but-not-negotiated server produce identical EE + Certificate bytes; the X.509 leaf DER is still carried). Tests: three cross-Onyx Server loopbacks (Ed25519 / ECDSA-P256 / RSA-2048) driving the real `tls_client` with `offer_raw_public_key` and empty trust anchors → server presents a bare SPKI → client accepts (no chain/anchors) → data round-trips both ways; the byte-identical default-off proof; a negotiated-mode unit test (EE echoes type 20, Certificate carries exactly the leaf's `spki_der`, framing exact, nothing trailing). Full suite green (6250 passed / 4 skipped / 0 failed, unfiltered); zig-code-reviewer: **Approve-with-nits** — no CRITICAL/HIGH/MEDIUM; all seven gates (byte-identical-off, no-trust-bypass, fail-closed parse + buffer sizing, no dangling `spki_der`/free-exactly-once, resumption byte-identical, own-cert parse-fail → internal_error with no panic, 0.17 idioms/no re-rolls) verified against source; symmetric with the shipped client (silent-X.509 is what the client expects). LOW-1 (add an RSA RPK loopback) applied; LOW-2 (strict malformed-list rejection matches the house `record_size_limit` pattern) accepted as-is. **`client_certificate_type` (raw-key CLIENT certs for mTLS) + daemon/TOML wiring deferred (follow-up).** **not deployed.** (Worktree base tripwire AGAIN: spawned stale at `50ca052` — `git reset --hard zig-0.17-port` to `d281594` before any work.)
- 2026-07-07: **0.3 BoGo shim IMPLEMENTED (reachable subset).** New `tools/bogo_shim.zig` + `zig build bogo-shim` step (and a self-driven `zig build bogo-shim-test`), kept OUT of `zig build test`. The shim speaks BoringSSL `ssl/test/runner`'s contract — dial the runner's TCP port, write the `-shim-id` LE prefix, drive Onyx Server's `TlsConn` server / `tls_client.Client` (certs via `tls_certs.loadOrBootstrap`) over a read→feed→write pump, XOR-echo app data, and exit **0**/**89**/**1** (synthesizing a fatal alert via the engine `takeAlert`/`alertRecordForError` on handshake failure). **Zero production-code changes** (`src/crypto`/`src/daemon` untouched — the shim reuses public/`ForTest` surfaces the daemon already exposes; purely additive in `tools/` + `build.zig`). Handled flags: `-server`/`-port`/`-shim-id`/`-cert-file`/`-key-file`/`-min-version`/`-max-version`/`-expect-version`/`-curves` (client 23→secp256r1, 4588→X25519MLKEM768)/`-select-alpn`/`-expect-alpn`; everything else (TLS<1.2, DTLS, resumption/0-RTT, mTLS, HRR, `-shim-writes-first`/`-shim-shuts-down`, client-role ALPN, other curves) ⇒ exit 89, documented. Self-driven proof (no external Go harness): **17/17** in `bogo-shim-test` — 12 pure parse/record-framing units + 5 subprocess smokes that spawn the REAL binary and drive it with Onyx Server's own loopback engines, asserting exit 0 (server 1.3 + ALPN; client 1.3; client `-curves 23`) / 89 (unimplemented flag) / nonzero (malformed ClientHello → alert), the Debug binary's safety checks turning any memory fault into a hard failure. External-harness wiring (pin BoringSSL, `tools/bogo.sh` + `-shim-config` DisabledTests/ErrorMap, CI `bogo` job) is the documented out-of-repo TODO. **Surfaced (NOT fixed — production untouched, out of 0.3 scope):** `TlsConn.onInbound` corrupts its engine union to a half-constructed `.tls12` if `tls12_server.Server.init` fails (`NoSigningKey`); the daemon never hits it (always-keyed `cfg12`), and the shim sidesteps it by running 1.3-only when the leaf key can't do 1.2. Full suite green (unfiltered **6245/6249, 4 skipped, 0 failed** — unchanged, no `src/` touched); `zig-code-reviewer`: **Approve-with-nits** (isolation confirmed zero `src/` diffs, memory-safety traced per engine ownership contract, flag/exit contract coherent, the RLS landmine independently reproduced + agreed out-of-scope; addressed the two worthwhile LOW nits — a `build.zig` comment accuracy fix + a larger `/proc/self/environ` read). **not deployed.**
