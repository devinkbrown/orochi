You are a world-class daemon architect with 100% design freedom. Design — from scratch, in Zig —
the DAEMON CORE, the comptime MODULE SYSTEM, and the FULL FEATURE-PARITY map for "Orochi", a
clean-slate Zig successor to the ophion IRC daemon. Everything is Zig-native; NO C interop. Read
the shared brief first: /home/kain/orochi/docs/BRIEF.md

MANDATE: invent. Replace ophion's MAPI v4 dynamic-.so machinery with something far better using
comptime. ophion is REFERENCE ONLY — mine it for the complete feature list and protocol behavior,
then design a cleaner architecture.

Study (cite file:line):
- Module/hook/dispatch core: /home/kain/ophion/include/modules.h, ircd/modules.c, ircd/hook.c,
  include/hook.h, ircd/parse.c
- Representative modules: modules/m_cap.c, modules/cap_message_tags.c, modules/cap_server_time.c,
  modules/sasl_plain.c, modules/sasl_scram.c, modules/m_ircx_base.c, modules/m_ircx_prop.c,
  modules/core/m_message.c, modules/m_chathistory.c
- Services: ircd/svc_account.c, ircd/svc_channel.c, ircd/services_db.c
- Python embedding: ircd/pymod.c, pymod_api.c, include/ophion_pyapi.h, python_modules/*.py
- The full module set under /home/kain/ophion/modules/ and modules/extensions/

Produce a thorough design doc "03 — Daemon Core, Comptime Modules & Feature Parity":
1. Daemon lifecycle in Zig: startup, config (replace TOML parsing — keep TOML? propose format),
   rehash, graceful drain, signal handling.
2. Comptime module/registry system: how a module declares COMMAND/HOOK/CAP/CHANMODE/NUMERIC/
   ISUPPORT entries; how the registry is assembled at compile time into dispatch tables with zero
   runtime registration cost. Decide statically-linked-comptime vs dynamic loading and JUSTIFY.
   Address feature flags, /REHASH, and optional features under the chosen model.
3. Typed hook system via comptime (replace void* hook_data with typed payloads); dispatch ordering;
   cap-gated emission expressed in types.
4. Client/Channel/Membership data model in Zig; command dispatch + parse pipeline.
5. FULL FEATURE INVENTORY TABLE — every feature group: core commands; ALL IRCv3 caps (batch,
   message-tags, server-time, account-tag, prop-notify, STS, bot, cap-notify, multiline,
   chathistory, read-marker, monitor, etc.); ALL SASL mechs; ALL IRCX modules (PROP, ACCESS,
   WHISPER, CREATE, auditorium, MODEX, AUTH, events, comic, etc.); services
   (NickServ/ChanServ/MemoServ/VHost); WebSocket; GeoIP; cloaking; spamfilter; bans. For each:
   assign {NATIVE-ZIG, COMPTIME-GENERATE, DROP-AND-REPLACE} + one-line rationale + effort S/M/L.
6. Scripting/extensibility: ophion embeds CPython — propose the Zig-native replacement (WASM
   plugins? embedded scripting? hot-reloadable Zig modules?). Recommend with cost/benefit.
7. Persistence: services DB (ophion uses SQLite) — keep SQLite, or a Zig-native store? Recommend.
8. NOVEL TECHNOLOGIES: 3-6 inventions (e.g. comptime-generated wire codecs from a schema, a typed
   capability/permission system, deterministic replay/testing harness, structured-concurrency
   command handlers). Name + spec.
9. Risks & open questions.

Include a concrete Zig comptime module-registration sketch. Be opinionated; cite real symbols.
PLANNING ONLY — do not modify any files.
